> **Superseded execution status (2026-09-15):** The owner authorized the complete
> [ready-to-share hobby build](ready-to-share.md), including S7 adoption. D8–D14/D22
> are integrated; the 18 CD foundation paths and remaining CD/UI/rip behavior are now
> integrated and focused-tested. The three reviewed libraries are installed with
> backups. The older checkpoint entries below are historical receipts, not current
> approval requirements. Current acceptance and deliverables are tracked in the new plan.

# Release licensing remediation

## Purpose / Big Picture

Prepare this separate local Songbird candidate for GPL-3.0-or-later distribution:
complete license delivery, independently generated geometric artwork, verified
corresponding source, exact publication payload checks, and Discogs attribution
and freshness across transient display and mutation boundaries. Preserve local
music, saved artwork/tags, BPM behavior, storage schema, persisted theme choices,
and the original development checkout. Technical completion is not legal clearance.

The owner authorized execution on 2026-09-15. The detailed specification below
adopts the supplied release-licensing remediation plan, including its L/A/S/P/D/V
task identifiers and proposed code. Historical references to a planning turn mean
the supplied specification was uncompiled, not that this active task is plan-only.
Execution approval does not satisfy the separate human gates below. No push,
installed-app access, ordinary library access, signing setup, or commit is authorized.

## Progress

- [x] 2026-09-15, owner name/privacy decisions: Songbird naming approved and
  privacy/contact review waived as release prerequisites, each with its own bound
  nonblocking decision. Acceptance first failed on those gates; then 17 focused
  gate tests and the 102-case Python suite passed (two optional rebuild skips).
  Fresh isolated quick passed 280 XCTest cases (two optional-store skips) and 304
  Swift Testing cases in 40 suites. Documentation and 1,667 build-input hashes
  passed. Current status docs and LICENSE_SCOPE evidence updated; original-code
  authority text is unchanged. Three gates remain: source correspondence,
  transient system display and notice delivery. Runtime code is unchanged.
  Evidence: /private/tmp/songbird-release-decisions-w23a88wz/.

- [x] 2026-09-15, owner metadata release decision: permanent imported metadata,
  saved filenames, maintenance reports and existing Undo are owner-nonblocking
  with their own bound decision. Combined acceptance first failed, then 17 focused
  gate tests and all 102 Python cases passed (two optional rebuild skips). Fresh
  isolated quick passed 280 XCTest cases (two optional-store skips) and 304 Swift
  Testing cases in 40 suites. Documentation and 1,667 build-input hashes passed;
  SwiftPM's unused-pin deletion/restoration stayed inside the disposable copy.
  The actual gate check accepts both retention decisions and still rejects five
  pending gates. Evidence: /private/tmp/songbird-metadata-decision-tmo0yqdq/.
  Runtime behavior is unchanged; other paused work stays paused.

- [x] 2026-09-15, owner artwork release decision: permanent Discogs covers are
  explicitly nonblocking; retained metadata/report policy remains separate. The
  focused acceptance assertion failed before implementation, then all 16 gate
  tests and the 101-case Python suite passed (two optional rebuild skips). Fresh
  isolated full quick passed 280 XCTest cases (two optional-store skips) and 304
  Swift Testing cases in 40 suites. Documentation checks passed. The real checker
  reports the artwork decision separately and still rejects the other six pending
  gates. This bounded change does not resume the paused implementation below.

Execution paused after the owner's time/scope correction. The assistant requested
the remaining UI-fixture worker stop; completed but unintegrated handoffs are retained,
not automatically applied. Earlier running-work statements below are historical
checkpoint status. The candidate is not release-ready and S7 remains unapproved.

- [x] 2026-09-15: Execution authorized; read candidate controls and plan standard.
- [x] 2026-09-15: Verified preparation payload: 536 recorded rows plus its manifest;
  no content/size/mode mismatch or eligible extra/missing path. Unborn main has no
  indexed files, refs, objects, remotes, or external Git linkage.
- [x] 2026-09-15: Preserved preparation manifest and original input/Git boundary
  evidence outside the candidate; established isolated home/test-root/scratch.
- [x] 2026-09-15: Baseline BPM passed six Swift Testing cases in one suite.
- [x] 2026-09-15, L0: Owner confirmed “Copyright (C) 2026 Andrew Zimmerman”
  and authority for original code/new artwork under GPL-3.0-or-later.
- [x] 2026-09-15, L1–L5 technical slice: Canonical texts, staging/resolver and
  About legal sheet implemented; 10 Python tests and seven Swift resolver tests
  passed after behavioral RED/GREEN. Full quick passed 187 XCTest cases (two
  optional-store skips) and 284 Swift Testing cases in 40 suites; optimized build
  passed. Unsigned legal staging/equality and packager syntax passed. Nested
  source notices remain S1's review gate; no rendered About acceptance is claimed.
- [x] 2026-09-15, A1–A5: Integrated exact tested path ledger; original renderer
  regenerated all 27 runtime images twice with identical bytes in the parent run.
  Removed legacy crops, changed only the neutral label, retired four screenshots.
  Combined quick passed 191 XCTest cases (two optional-store skips), 284 Swift
  Testing cases in 40 suites; optimized build passed. Actual UI review remains V2.
- [x] 2026-09-15, S1/S2/S4 research: Recorded four blocked source components,
  verified all retained aubio source/header bytes and exact Ooura derivation, and
  delivered recovered nested notices. Original FLAC/ogg supplier chain unrecovered;
  controlled replacements approved, not adopted. See docs/vendor-source-review.md.
- [x] 2026-09-15, S6: Integrated 169 pinned upstream GRDB files (only the approved
  test-target removal changes upstream content), two supplemental notices and
  opt-in local dependency selection. Parent reverified upstream blobs and ran
  fresh offline and OS-network-denied release builds: all 164 GRDB Swift sources
  and privacy resource compiled/copied, no dependency checkouts/downloads. Ordinary
  Package.resolved stayed unchanged in the candidate; resolver deletion/restoration
  occurred only in the independent test copy. Final S8 archive acceptance is separate.
- [x] 2026-09-15, S3/S5: Integrated the non-destructive shared rebuild boundary,
  all 982 official FLAC/ogg source files and exact notices, with safe delivered
  permissions/original archive modes recorded. Parent ran all ten option/source/
  actual-build tests. New outputs passed current-source isolated focused tests,
  full quick (244 XCTest including one identity probe; two optional-store skips;
  284 Swift Testing), optimized build, 25-case audio TSan, dyld path/hash checks,
  and 8192-frame bit-exact native/Ogg FLAC round-trips with metadata.
- [ ] S7 approval: The exact parent-built output hashes were presented to the
  owner; the prompt was unanswered. Candidate dylibs remain untouched. Do not
  adopt based on silence or substitute similarly named child/intermediate outputs.
- [ ] S7/S8: Actual-output adoption approval and final corresponding-source archive rebuild.
- [x] 2026-09-15, P1–P3 helpers: Integrated and parent-tested 16 primitive, 37 phase
  and 11 gate tests. All Python tests including legal delivery: 75 passed. Six
  pending release gates block as expected; the unsealed payload is rejected.
- [x] 2026-09-15, P2 checkpoint ledger: publication/changes.json records only the
  exact reviewed local L/A/P/S-research paths and preserves original preparation
  provenance. It is not a release manifest or permission for unlisted changes.
- [ ] P4: Extend/review the ledger for final source/Discogs inputs and reseal only
  after current build/source/artifact evidence is integrated. No automatic old sealer.
  Before the seed, review and normalize candidate payload permissions to Git's
  non-executable/executable 0644/0755 contract, preserving original preparation
  modes in provenance. The checked source copy still has 29 inherited 0600 files;
  exact development-mode checks would otherwise reject a normal Git checkout.
  Perform this only after isolated overlays are merged, then refresh affected
  component/gate hashes and verify a disposable checkout/archive round trip.
- [x] 2026-09-15, D1–D7: Integrated exact tested foundation/cache-consumer ledger;
  full candidate quick passed 243 XCTest cases (two optional-store skips) and 284
  Swift Testing cases, optimized build passed. Actual audio TSan on an exact frozen
  current-source copy passed 25 cases. This does not close loaded-state/mutation
  consumers. D8–D14 and D22 are running in independent snapshots.
- [ ] D8–D24: Complete remaining loaded UI/CD/thumbnail/queue/rip/remediator behavior,
  final mixed-path regressions and whole-feature acceptance.
- [ ] V1: Current combined Python/Swift quick/optimized/offline checks and audio TSan when applicable.
- [ ] V2: Separately approved disposable package/UI acceptance and canned-response wiring.
- [ ] P5/P6/V3: Separately approved clean root/index/history and source archive acceptance.

## Surprises & Discoveries

No candidate drift was found at entry. The preparation manifest is a historical
snapshot, not a release manifest to reseal automatically after edits. The current
candidate controls say no license was selected during baseline preparation; that
historical statement must not conceal the later owner-selected GPL route. Final
copyright wording and authority were subsequently confirmed for L0; that approval
does not establish third-party source correspondence or branding rights. The read-only GRDB checkout is clean at
2cf6c756e1e5ef6901ebae16576a7e4e4b834622. Swift reports Apple Swift 6.3.3 on arm64.

## Decision Log

- 2026-09-15, owner: "Privacy/contact review" is unnecessary; "Songbird name"
  approved. Keep the Songbird name and record both gates as owner-nonblocking with
  separate bound decisions. This supersedes the earlier pending brand/privacy
  prerequisites. Trademark clearance remains unconfirmed and no privacy/contact
  review is claimed. Source correspondence, notice delivery, transient system
  display acceptance and final publication authorization remain separate.

- 2026-09-15, owner metadata retention decision: the owner agreed imported
  information such as an album year must remain after lookup expiry, accepting
  the proposed permanent tags/filenames/reports/Undo policy and nonblocking release
  treatment. Record `discogs-persistent-metadata-policy` as `owner-nonblocking`
  with its own hashed decision. Provider permission remains unconfirmed. This
  supersedes earlier pending G-IMPORT retention instructions; source acquisition,
  transient expiry and the approved system-display policy are unchanged. No
  existing data migration, runtime deletion, vendor adoption or publication is
  part of this change.

- 2026-09-15, owner release decision: uncertainty over permanent Discogs cover
  importing must not block release. Split the old combined import gate into
  `discogs-permanent-artwork-policy` (owner-nonblocking, provider permission
  unconfirmed) and `discogs-persistent-metadata-policy` (pending at that checkpoint).
  The exact owner decision is publication/discogs-artwork-decision.json. The later
  metadata decision above extends this status to its own bound evidence. Other gates and
  publication authorization are unchanged. This supersedes artwork-blocking
  instructions and the combined import-gate identifier in the adopted templates
  below. Provider contact remains optional and has not occurred.
- 2026-09-15, owner clarification: search results may be temporary; saved album
  artwork must remain permanent and available offline. The owner then selected
  keeping Discogs image importing. Preserve the existing acquisition and saved-cover
  behavior; establish applicable terms or permission for that workflow. Provider
  switching is not the selected remedy. This does not approve a provider
  license interpretation, change G-DISPLAY for transient CD art, or authorize a
  schema migration. Research and an unsent provider question are recorded in
  docs/DISCOGS_CONTENT_POLICY.md. The later owner release decision above supersedes
  the earlier requirement to resolve artwork permission before release.
- 2026-09-15, owner: Execute the supplied plan in this candidate only; retain its
  separate approval gates. No original-checkout writes.
- 2026-09-15, adopted owner choices: Keep aubio and GPL-3.0-or-later for authorized
  original code/new artwork; replace unverified images; retain Songbird naming and
  persistent Discogs imports while their public-release policies remain pending.
- 2026-09-15, implementation: Reconcile the detailed plan into this format before
  product edits; keep baseline and RED/GREEN logs outside the publication tree.
- 2026-09-15, owner: Confirmed exact L0 notice and original-code/new-art authority;
  recorded in docs/LICENSE_SCOPE.md. No blanket relicensing of third-party material.
- Pending human decisions: controlled binary adoption (S7) and the exact first
  local root commit (P5). Distribution/signing identities/notarization remain excluded.
- Owner-approved Songbird naming, waived privacy/contact review, saved Discogs
  artwork and metadata/report retention are independently owner-nonblocking.
  The approved system-display fallback still needs D20
  implementation/acceptance before its release gate can be verified.
- 2026-09-15, owner, S5: Approved controlled source import/build from official
  FLAC 1.5.0 and libogg 1.3.6 archives for arm64/macOS 14+. This deliberately
  narrows the currently universal FLAC/ogg outputs to the app's aubio-supported
  architecture. It does not approve adopting any produced binary (S7).
- 2026-09-15, owner, S6: Approved exact pinned GRDB production-source vendoring
  with a documented minimal manifest adaptation omitting upstream tests/resources.
  Preserve production code, CSQLite interfaces, PrivacyInfo and nested notices;
  retain Songbird's tests. The unverified upstream test image is not imported.
- 2026-09-15, owner, G-DISPLAY: Approved original CD-Text/default metadata and no
  transient Discogs artwork in system Now Playing, which cannot carry the source
  link. Fresh attributed content remains available inside Songbird. No historic
  saved-content rewrite is authorized. See docs/DISCOGS_CONTENT_POLICY.md.
- 2026-09-15, owner, V2: Approved later disposable local packaging, ad-hoc signing
  and launch for synthetic UI acceptance. No installed/normal app/library, Developer
  ID/notarization, live service credentials or uncontrolled browser navigation.
- 2026-09-15: Rechecked the primary Discogs API terms (page updated May 27, 2025).
  Recorded current freshness/attribution distinctions and added the required
  non-affiliation/trademark notice to usage and canonical legal documentation.
- 2026-09-15, S7: No response to the exact-output adoption request. Await explicit
  approval for parent outputs under the run's vendor-parent-builds directory:
  aubio ff371b8123f1d9e6985e5d096324e37caa9d1cfc746d8f2e34c93ec4f124ff9c;
  FLAC 59475d5011cda5a09e9a0486181273a0a348273c680af812cfe14e2c2bc27a64;
  ogg 6c79d0c7fd244e0dd329fcb172b3b9722251c5bc689d88228e1b954754a6c696.
  Before adoption, require unchanged candidate before hashes and new external
  backups; use the tested script's exact-hash operation, then read back each target.

## Context and Orientation

The implementation root is this repository (CANDIDATE). CANDIDATE_PARENT is its
parent; ORIGINAL is the separate preserved development checkout, read-only.
Package.swift defines SongbirdLib, the Songbird executable, independent retained
Dock support, C bridges and diagnostic products. Sources/Resources is copied by
SwiftPM; the packager's existing top-level loop omits nested Legal resources.
ArtworkThumbnailService converts logo fallbacks into silhouettes, so those aliases
must be monochrome transparent marks rather than app-icon tiles. Current Dock
crops are calibrated to old resources and must be removed without changing choices.

Sources/Discogs owns requests/search caches and review UI. OpticalDisc, artwork
thumbnails, year/genre flows and maintenance are additional consumers: helper tests
alone do not establish complete expiry behavior. LibraryItemActionHandler remains
the shared UI mutation boundary. No stored SwiftData fields or historical schema
edits are part of this plan; runtime CD evidence uses transient properties only.

Vendor source labels and Mach-O ABI versions are separate facts. The retained
source and exact rebuild outputs require authenticated source/license evidence;
never infer source pins from a dylib version. GRDB remains retained and licensed
under MIT. Use scripts/aubio-cmake, not the reduced upstream root recipe.

## Plan of Work

Resolve L0, then execute L1–L5 and A1–A5 with focused behavioral RED/GREEN checks.
Investigate S1–S8 and implement P1–P4 before any full-tree root commit. Block the
specific source/adoption phase if evidence or approval is missing; independent
technical slices may continue locally without changing the corresponding gate.
D1–D24 are incremental service slices, but all consumers are mandatory for final
acceptance. No generic commit-after-step rule overrides the clean-root gate.

Use each task's exact file scope, tests and dependencies from the adopted detailed
specification. For missing typed APIs add only the minimum compileable shell, then
observe the intended failing assertion before implementation. Do not treat a missing
symbol, missing dependency fixture, zero test count or old run as behavioral RED/GREEN.

## Concrete Steps

Run from this candidate, with fresh external evidence directories. For this pass
an isolated run directory was already created; reuse the existing shell RUN rather
than creating a new scratch build for every test. Recovery setup for a new pass:

```sh
CANDIDATE="$PWD"
CANDIDATE_PARENT="$(dirname "$CANDIDATE")"
RUN="$(mktemp -d /private/tmp/songbird-licensing.XXXXXX)"
mkdir -p "$RUN/home" "$RUN/test-root" "$RUN/logs"
export CANDIDATE CANDIDATE_PARENT RUN
export HOME="$RUN/home" CFFIXED_USER_HOME="$RUN/home"
export SONGBIRD_UI_TEST_ROOT="$RUN/test-root" SONGBIRD_UI_TESTING=1
export PYTHONDONTWRITEBYTECODE=1
export GRDB_SOURCE="$CANDIDATE_PARENT/songbird-public-verification/scratch/checkouts/GRDB.swift"
quick() { ./check.sh quick --scratch-path "$RUN/scratch" "$@"; }
quick --filter BPMDetectionTests
```

L0 wording/authority is now owner-confirmed; do not substitute Git author metadata
for that recorded approval or extend it to third-party rights.
Detailed tasks and templates follow after the required plan sections. They specify
new commands, not already-existing helpers. Do not run a proposed verifier before
implementing and testing it. For S5, record release-specific authenticated source
commands before running a replacement build; for V2, specify and test canned-response
UI wiring before replay. No Keychain or real API token is needed for deterministic tests.

## Validation and Acceptance

### S5 approved source-specific build specification

Source selections are controlled replacements, not recovered old binary provenance.
The following downloaded archive SHA-256 values were independently rechecked and
match the official https://xiph.org/downloads/ listing:

- https://downloads.xiph.org/releases/flac/flac-1.5.0.tar.xz:
  f2c1c76592a82ffff8413ba3c4a1299b6c7ab06c734dee03fd88630485c2b920
- https://downloads.xiph.org/releases/ogg/libogg-1.3.6.tar.xz:
  5c8253428e181840cd20d41f3ca16557a9cc04bad4a3d04cce84808677fa1061

Review archive paths/types before source import. Preserve applicable source notices
(FLAC includes BSD/GPL/LGPL/FDL material; it is not uniformly BSD). Keep build/install
outputs external, with fresh noncolliding build/output/evidence directories. Source
import paths are Vendor/FLAC/source/flac and Vendor/FLAC/source/ogg. The reviewed
release CMakeLists and FindOgg.cmake support these explicit controls:

```sh
mkdir -p "$RUN/empty-pkgconfig"
export PKG_CONFIG_PATH="" PKG_CONFIG_LIBDIR="$RUN/empty-pkgconfig"
cmake -S "$PWD/Vendor/FLAC/source/ogg" -B "$RUN/ogg-build" \
  -DCMAKE_BUILD_TYPE=Release -DCMAKE_OSX_ARCHITECTURES=arm64 \
  -DCMAKE_OSX_DEPLOYMENT_TARGET=14.0 -DCMAKE_EXPORT_COMPILE_COMMANDS=ON \
  -DCMAKE_FIND_USE_PACKAGE_REGISTRY=OFF -DCMAKE_FIND_USE_SYSTEM_PACKAGE_REGISTRY=OFF \
  -DBUILD_SHARED_LIBS=ON -DBUILD_FRAMEWORK=OFF -DBUILD_TESTING=OFF \
  -DINSTALL_DOCS=OFF -DINSTALL_PKG_CONFIG_MODULE=OFF -DINSTALL_CMAKE_PACKAGE_MODULE=OFF \
  -DCMAKE_INSTALL_PREFIX="$RUN/ogg-stage" -DCMAKE_INSTALL_LIBDIR=lib \
  -DCMAKE_INSTALL_NAME_DIR=@rpath -DCMAKE_BUILD_WITH_INSTALL_NAME_DIR=ON
cmake --build "$RUN/ogg-build" --config Release --parallel
cmake --install "$RUN/ogg-build" --config Release
cmake -S "$PWD/Vendor/FLAC/source/flac" -B "$RUN/flac-build" \
  -DCMAKE_BUILD_TYPE=Release -DCMAKE_OSX_ARCHITECTURES=arm64 \
  -DCMAKE_OSX_DEPLOYMENT_TARGET=14.0 -DCMAKE_EXPORT_COMPILE_COMMANDS=ON \
  -DCMAKE_FIND_USE_PACKAGE_REGISTRY=OFF -DCMAKE_FIND_USE_SYSTEM_PACKAGE_REGISTRY=OFF \
  -DBUILD_SHARED_LIBS=ON -DBUILD_CXXLIBS=OFF -DBUILD_PROGRAMS=OFF \
  -DBUILD_EXAMPLES=OFF -DBUILD_TESTING=OFF -DBUILD_DOCS=OFF -DINSTALL_MANPAGES=OFF \
  -DINSTALL_PKGCONFIG_MODULES=OFF -DINSTALL_CMAKE_CONFIG_MODULE=OFF \
  -DCMAKE_DISABLE_FIND_PACKAGE_Intl=ON \
  -DWITH_OGG=ON -DENABLE_MULTITHREADING=ON \
  -DWITH_FORTIFY_SOURCE=ON -DWITH_STACK_PROTECTOR=ON \
  -DOGG_INCLUDE_DIR="$RUN/ogg-stage/include" -DOGG_LIBRARY="$RUN/ogg-stage/lib/libogg.dylib" \
  -DCMAKE_INSTALL_PREFIX="$RUN/flac-stage" -DCMAKE_INSTALL_LIBDIR=lib \
  -DCMAKE_INSTALL_NAME_DIR=@rpath -DCMAKE_BUILD_WITH_INSTALL_NAME_DIR=ON
cmake --build "$RUN/flac-build" --config Release --parallel
cmake --install "$RUN/flac-build" --config Release
```

These commands are specified from inspected release source, not yet a reported pass.
The controlled build subsequently exposed FLAC's auxiliary getopt target calling
optional find_package(Intl) even with programs disabled. The verified correction
above disables that unneeded host gettext lookup rather than weakening compile/link
leakage checks. The first failed configure evidence is retained externally.
The official source archives also carry 0666/0777 modes. For these newly imported
vendor files, strip group/other write permission immediately while copying to the
candidate (0644 or 0755 according to executable bits), rather than introducing
world-writable build inputs. Preserve original archive modes in the import ledger
as explicit metadata transformations; source bytes and notices remain unchanged.
The rebuild admission inventory must describe the actual safe delivered modes.
Existing candidate 0600 normalization remains deferred until overlapping work ends.
Implement/test the non-destructive rebuild script boundary before executing them;
record its equivalent exact argv, effective environment, source/config/toolchain and
pre/post install-name output hashes. Verify CMake's actual OGG paths and all Mach-O
linkage/architecture/deployment targets so a host library cannot masquerade as the
controlled dependency. Do not use an Ogg_DIR-only lookup. Actual-library tests and
S7 approval still precede any candidate binary adoption. Run isolated rebuild tests
and record any needed command correction before claiming source correspondence.

Focused tests and full ./check.sh quick must run against the changed tree; build
optimized products and exercise offline source delivery after its implementation.
Run Python resource/archive/gate tests and actual unsigned legal staging; reject
stale/missing/link/extra files rather than hiding them. Run isolated audio TSan
when backend, CD/playback-adjacent behavior or vendor artifacts change. Hardware
and rendered UI acceptance require their actual evidence, not source inspection.

The release gate checker must return nonzero while any human/source/privacy gate
is pending. A passing archive checker proves membership and bytes, not permission.
Do not create a full-tree seed containing retired images/screenshots or unreviewed
vendor binaries. The first root commit needs owner approval of its exact index;
no push or distribution is authorized here.

## Idempotence and Recovery

Never reset or clean either checkout. Generate/build in fresh external destinations
and refuse collisions. Keep before hashes, preparation manifest and test receipts
outside the public tree. Replace only explicitly named generated resource files;
never copy old art back into public history. Keep existing saved covers/tags and
Undo data. Do not adopt vendor outputs without approval and a reviewed hash/backup.
Compare candidate changes to the saved preparation manifest, not an unborn HEAD.
Update this plan, CONTINUITY.md and its single NEXT_STEPS.md link as facts change.

## Artifacts and Notes

External evidence for this pass is under /private/tmp/songbird-licensing.ia40aT:
pre-execution-boundaries.json, preparation-manifest.json, and logs/baseline-bpm.log.
It is not product source and must not be added to the candidate. The baseline
command passed six BPM cases. logs/L1-* and logs/L2-* record Python behavioral
RED/GREEN; logs/L4-* record resolver RED/GREEN. logs/L5-focused.log and
logs/L5-release.log record the legal slice; logs/legal-python-final.log and
logs/legal-quick.log record combined checks.
Integrated checkpoint logs: LAP-python.log, LAP-final-quick.log and
LAP-final-release.log. verify-LAP-checkpoint.py verified original input/Git/index
preservation and the explicit changed path union; LAP-checkpoint-paths.json holds
actual before/after rows outside the public tree. It also scanned all then-eligible
files for retired hashes and found none. The public changes ledger intentionally
records provenance/path authorization rather than self-referential content hashes.
The supplied detailed plan remains preserved in the original checkout; its code
examples are proposed and must be checked against current declarations before use.

## Interfaces and Dependencies

LegalDocument/LegalResources resolve packaged text lazily before SwiftPM resources.
SongbirdArtworkCatalog shares compatibility basenames/colors with the standalone
AppKit generator and independent Dock renderer. DiscogsFetchStamp and
DiscogsContentEvidence carry original acquisition time, a sleep-inclusive monotonic
clock and boot identity; maximumAge is strictly less than five hours on both clocks.
Nil evidence is local data, not permission for Discogs callers to erase provenance.
Freshness must survive every await/cache/image normalization/actor and final save.

LibraryArtworkChange gets optional evidence; expiry is a typed rollback-preserving
mutation error. Search/CD caches share a Discogs-only stamped cache kernel; generic
MusicBrainz/local artwork behavior remains unchanged. The resource and publication
helpers use Python standard library only. Source/offline selection retains GRDB's
ordinary remote lockfile and supplies its exact verified source separately.

## Outcomes & Retrospective

L0 and the L1–L5 technical slice are implemented and tested, including full quick
and optimized compilation. The About sheet still requires V2 rendered acceptance.
Artwork and publication helpers are integrated and verified, including parent
regeneration and combined quick/release checks. Recovered nested notices and source
review records are installed; all four component statuses remain blocked. Approved
S6 and foundational D1–D7 are integrated; current normal quick/release and 85 Python
tests pass. Parent fresh offline/network-denied builds and 25-case audio TSan pass
against the verified frozen integrated compiler inputs. S3/S5 are integrated and
replacement outputs are tested; S7 adoption is awaiting explicit approval. D8–D14/D22
and CD/service/thumbnail foundations continue in separate disposable snapshots.
No candidate vendor binary, index, ref or remote has changed. PUB-REMEDIATION
remains active for remaining S/P/D and final combined acceptance. Source,
transient Discogs display, notices, UI/hardware and publication acceptance remain
open. The later owner naming/privacy/import decisions above are nonblocking.
The optional AGENTS.md status edit was blocked by an unanswered protected-file
approval prompt; it was not retried. Its baseline-specific statement remains historical.

## Detailed adopted task specification

The following is the supplied exact task/appendix contract, with local checkout
paths parameterized. Planning-only descriptions qualify its templates as unexecuted
examples; current execution status is exclusively in Progress above. Human gates
and prohibited original/private-data operations remain in force.

##### L — Licensing scope and legal-resource delivery

| Task | Exact files and change | RED → GREEN verification and checkpoint |
|---|---|---|
| L0 | `docs/LICENSE_SCOPE.md`, `NOTICE.txt`: confirm the copyright-holder wording with the owner. Record authorized original-code scope, new-artwork GPL-3.0-or-later, imported aubio GPL, FLAC headers/BSD, GRDB/MIT, and existing CC0 fixture exception. No assignment/employer permission is invented. | Human gate: owner confirms the text/authority or it stays pending. No copyright-holder placeholder may enter a release. This does not require a new CLA system. |
| L1-RED | Add `scripts/tests/test_legal_inputs.py` using the real-input test in Appendix L. It reads the existing aubio COPYING, root documents, and the verified GRDB checkout via an explicit environment path. | `python3 -m unittest discover -s scripts/tests -p test_legal_inputs.py -v` must fail because root GPL/notice documents are absent, not because a dependency fixture path is silently wrong. |
| L1-GREEN | Add `LICENSE`, `NOTICE.txt`, `THIRD_PARTY_NOTICES.txt`, `SOURCE_CODE.md`, and `docs/LICENSE_SCOPE.md`. Copy the entire GPL text verbatim from `Vendor/aubio/src/COPYING`; use the full verified GRDB MIT notice and existing FLAC/ogg notice verbatim. Preserve aubio's own notices and add established nested notices. | Repeat L1 command. Expected: all assertions pass; Ooura/source-evidence review remains S1's blocker, not a synthesized notice. The full GPL copy is the license text; NOTICE explicitly selects version 3-or-later for authorized original portions. |
| L2-RED | Add `scripts/tests/test_legal_resources.py` from Appendix L. Introduce only the importable helper API shell until the stage/copy equality test fails. Then add stale-copy, missing/extra/symlink, and destination-collision cases one at a time. | `python3 -m unittest discover -s scripts/tests -p test_legal_resources.py -v`; require the intended failed assertions before the full helper implementation. |
| L2-GREEN | Add `scripts/legal_resources.py` from Appendix L and four canonical resource copies under `Sources/Resources/Legal/`. The root documents remain canonical; no separate hand-maintained paraphrases. | Run L2 command; then `python3 scripts/legal_resources.py check . Sources/Resources/Legal`. Expected: exit 0 and exact byte equality. |
| L3 | In `scripts/package-songbird.sh`, insert the Appendix L stage invocation after the existing top-level resource loop and before signing. Do not change signing logic, default identities, or deployment options. Keep the two existing standalone vendor notices for this first localized change. | Stage into `"$RUN/package-fixture/Contents/Resources/Legal"` with the exact helper command below and run `check` there. Missing/stale source texts must fail before a legal destination is created. This proves the shared copy helper, not a fresh signed app. |
| L4-RED/GREEN | Add `Tests/SongbirdTests/LegalResourcesTests.swift`, then `Sources/Utils/LegalResources.swift` from Appendix L. The resolver must prefer packaged text, never evaluate `.module` for an incomplete packaged `.app`, and reject empty/unreadable text. | `quick --filter LegalResourcesTests`: observe behavioral RED with the minimal resolver shell, then GREEN. Add each lazy-fallback/error case separately. Include the actual SwiftPM resource-loading test. |
| L5 | Extend `Sources/Views/AboutSongbirdView.swift` using Appendix L's complete state/modifiers/private sheet. Reuse the existing About command at `Sources/App/SongbirdApp.swift:180–184`; do not add a duplicate menu. Add `legal.showDocuments`, `legal.contents`, and `legal.close` accessibility identifiers. | `quick --filter LegalResourcesTests`, then `swift build -c release --scratch-path "$RUN/scratch"`. Actual disposable About interaction is V2's separate gate; passing resource tests is not a UI claim. |

`NOTICE.txt`, after confirmed copyright wording, must include these exact substantive paragraphs (the first copyright line is owner-confirmed, not guessed):

```text
Songbird's authorized original application code and newly authored replacement artwork are licensed under GPL-3.0-or-later: the GNU General Public License as published by the Free Software Foundation, either version 3 of the License, or (at your option) any later version.

This program is distributed in the hope that it will be useful, but WITHOUT ANY WARRANTY; without even the implied warranty of MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE. You may redistribute and modify it under the applicable GNU General Public License terms. See LICENSE for the full license and SOURCE_CODE.md for corresponding-source information.

Third-party components retain their own copyright notices and applicable license terms; see THIRD_PARTY_NOTICES.txt and docs/LICENSE_SCOPE.md. Existing synthetic fixtures retain their declared CC0 terms. No trademark permission is granted or claimed by this notice. Songbird name clearance remains pending.
```

`SOURCE_CODE.md` initially says, truthfully, that this is a local development candidate and no public binary release/source-download URL exists. It lists the exact supported build entry points and pending source evidence. Before binary distribution, replace that development statement with actual release-specific source-download directions; never insert a made-up GitHub repository or release URL.

Exact resource synchronization and unsigned staging commands, after the canonical texts exist:

```sh
mkdir -p Sources/Resources/Legal
cp LICENSE Sources/Resources/Legal/GPL-3.0.txt
cp NOTICE.txt Sources/Resources/Legal/Songbird-NOTICE.txt
cp THIRD_PARTY_NOTICES.txt Sources/Resources/Legal/THIRD-PARTY-NOTICES.txt
cp SOURCE_CODE.md Sources/Resources/Legal/CORRESPONDING-SOURCE.txt
python3 scripts/legal_resources.py check . Sources/Resources/Legal
python3 scripts/legal_resources.py stage . "$RUN/package-fixture/Contents/Resources/Legal"
python3 scripts/legal_resources.py check . "$RUN/package-fixture/Contents/Resources/Legal"
```

Repeat synchronization whenever canonical texts change. Do not commit stale generated copies.

##### S — Corresponding source and controlled output provenance

This phase must not pretend missing provenance is already known. It is a complete decision procedure with explicit stop conditions: if supplier/source evidence cannot be authenticated, no source archive or full-tree initial commit containing those unreviewed binaries is authorized. Independent implementation work may continue locally while the gate is pending.

| Task | Exact files / action | Verification and expected result |
|---|---|---|
| S1 | Add `publication/corresponding-source.json` with exactly `aubio`, `flac`, `ogg`, and `grdb.swift` component records. Add `docs/vendor-source-review.md`. Start all source-correspondence statuses as `blocked`; unknown upstream IDs/hashes stay null. Inspect `Vendor/aubio/src/src/spectral/ooura_fft8g.c` and its authenticated upstream licensing context before treating the root GPL as its complete notice record. | P3's validator must report the blocked IDs. A record becomes verified only after source/license review and real rebuild evidence. There is no default “verified” generator. |
| S2 | For aubio, inventory every file in `Vendor/aubio/src/`, `Sources/AubioBridge/`, and `scripts/aubio-cmake/`; compare against an authenticated upstream candidate and record exact local differences/omissions in `docs/vendor-source-review.md`. | The upstream 0.5.0-alpha label and local dylib 5.4.8 are separate fields. Any missing grant/notice or unexplained incorporated code remains a hard gate. An exact retained source snapshot can identify source without inventing a Git revision. |
| S3-RED | Add `scripts/tests/test_rebuild_aubio_options.py`: execute the future `scripts/rebuild-aubio.sh --validate-options --output-dir TEMP --evidence-dir TEMP` with fake executable discovery, assert exit 0 and no vendor-file change; assert missing/relative/colliding output destinations fail before a compiler starts. | `python3 -m unittest discover -s scripts/tests -p test_rebuild_aubio_options.py -v`. Current script must not be run against the vendor binary for RED; use a disposable script-root fixture containing a sentinel dylib. |
| S3-GREEN | Change only `scripts/rebuild-aubio.sh`'s argument/output/evidence boundary: default to non-destructive explicit outputs, retain build directory, configure with `CMAKE_EXPORT_COMPILE_COMMANDS=ON`, record generated config, source/config hashes, toolchain, command argv and pre/post-install-name hashes. Adoption becomes a separate `--install-reviewed-output FILE` operation guarded by exact output hash and a fresh backup. | Repeat S3 tests. Run the actual custom CMake commands below in fresh directories, then test the actual new output in an isolated copy. Do not keep distributing the old binary simply because a new build passes. |
| S4 | Recover source/supplier records for the existing FLAC/ogg artifacts. FLAC's binary string narrows the candidate to 1.5.0; ogg 0.8.6 is an ABI/library version, not a source-release pin. Record authenticated source archives/revisions, their hashes, patches, build commands, and notices under `publication/corresponding-source.json` and `docs/vendor-source-review.md`. | If the original chain is recovered, verify delivered bytes against the supplier artifact and exact recorded inputs. Otherwise stop this branch and use S5. Do not fetch “latest” and label it the original source. |
| S5 | Controlled-replacement branch only: obtain owner approval for exact authenticated FLAC/ogg source releases, then add them under `Vendor/FLAC/source/flac/` and `Vendor/FLAC/source/ogg/` with their notices. Add `scripts/rebuild-flac-ogg.sh` using those releases' verified native build instructions, explicit source/output/evidence directories, and selected architectures. Build FLAC against that selected ogg. | No release IDs, download hashes, configure flags, or source URLs are supplied here because they are not established facts. The implementer must record those real inputs and update this active plan with copy-pasteable release-specific commands before running S5. This is an explicit evidence gate, not permission to guess build APIs. Preserve old candidate binaries outside public history until approved adoption. |
| S6 | Import GRDB's verified source revision without `.git` into `Vendor/GRDB.swift/`; retain all required source, manifest, resources, and notices. Review any upstream extra fixtures before including them; do not omit required target paths speculatively. Add the explicit `SONGBIRD_OFFLINE_DEPS=1` dependency branch in `Package.swift` from Appendix L. | `env -u SPI_BUILDER SONGBIRD_OFFLINE_DEPS=1 swift build -c release --scratch-path "$RUN/offline-scratch"` must build from the supplied source without fetching GRDB or a documentation plugin. Keep ordinary Package.resolved unchanged. Verify every vendored GRDB source row against that revision before marking it supplied. |
| S7 | Test controlled new aubio/FLAC/ogg outputs in an isolated candidate source copy, then adopt only the recorded artifacts after approval. Include exact source/config/build rows and before/after binary hashes in each component record. | Run `quick --filter 'BPMDetectionTests|NativeAudioBackendTests|TagWriterServiceTests'` against the copied build's actual libraries; run the full quick suite, optimized build, and `scripts/test-audio-tsan.sh` with isolated home. Check `otool -L`/architecture/install names and FLAC/ogg decode/metadata fixtures. Audio-source replacement is higher risk than notice copying. |
| S8 | Write final `SOURCE_CODE.md` build prerequisites and exact offline rebuild procedure from recorded commands. Keep Xcode/Apple SDK/system-library requirements documented, not copied proprietary framework binaries or private signing keys. Synchronize Legal/CORRESPONDING-SOURCE.txt. | Build the selected source archive in a new directory with no original checkout or prior `.build` dependency. Preserve the actual command log and output hashes. Independent recompiles need not have identical UUID/hash; source/input identity and the recorded build-to-delivered-output chain must match. |

The known, non-destructive aubio CMake invocation is:

```sh
cmake -S scripts/aubio-cmake -B "$RUN/aubio-build" \
  -DAUBIO_SOURCE_ROOT="$PWD/Vendor/aubio/src" \
  -DCMAKE_BUILD_TYPE=Release \
  -DCMAKE_OSX_ARCHITECTURES=arm64 \
  -DCMAKE_OSX_DEPLOYMENT_TARGET=14.0 \
  -DCMAKE_EXPORT_COMPILE_COMMANDS=ON \
  -DCMAKE_FIND_USE_PACKAGE_REGISTRY=OFF \
  -DCMAKE_FIND_USE_SYSTEM_PACKAGE_REGISTRY=OFF
cmake --build "$RUN/aubio-build" --config Release --parallel
```

Expected after the source/notice gate: a versioned aubio dylib in the build directory, compile_commands.json and generated config.h. Locate the actual output with a bounded file inventory, record its exact path, and use that exact file for later adoption. Do not run the reduced upstream root CMakeLists that refers to missing examples/tests.

A component record has `id`, `license`, `status`, `observed_version`, `upstream_identity`, `source_root`, `source_files`, `snapshot_sha256`, `build_files`, `binary_files`, and `evidence_files`. All file rows have exactly `path`, `sha256`, `bytes`, `mode`. Use the Appendix P record function to calculate values from real files. For statically linked GRDB use `linkage: "static"` and an empty `binary_files` list in the source component record; bind the actual Songbird executable hash in the separate external release receipt, not a fictional standalone GRDB dylib. Evidence must distinguish source-archive paths from external build-output receipt paths; do not copy local absolute-path logs with private details into public source blindly.

##### P — Exact payload, archive, and initial-commit gates

| Task | Exact files / action | RED → GREEN verification |
|---|---|---|
| P1 | Add `scripts/tests/test_publication_checks.py`, then `scripts/publication_checks.py` from Appendix P. Keep exact relative-path, duplicate, symlink/hardlink, missing/extra, byte and mode assertions. | `python3 -m unittest discover -s scripts/tests -p test_publication_checks.py -v`; first meaningful RED via minimal helper shells, then each corrupt archive case must be rejected and the complete synthetic archive accepted. No extraction into the working tree. |
| P2 | Add `publication/changes.json` containing the exact approved added/modified/removed paths and truthful origins. Preserve the original-copy provenance; do not turn the entire source tree into broadly “adaptable” inputs. Add candidate-owned `scripts/verify-publication.py` using Appendix P primitives and the phase contract below. Do not edit or depend on the old sibling sealer as the release tool. | New `scripts/tests/test_verify_publication.py` fixtures exercise pre-seed/index/development phases, command failure, unlisted file, wrong mode/blob, and removed file still present. `python3 -m unittest discover -s scripts/tests -p test_verify_publication.py -v`. |
| P3 | Add `publication/release-gates.json` and `scripts/check-release-gates.py` using Appendix P. Gate records start pending and contain exact evidence-file rows only after real review. | `python3 scripts/check-release-gates.py .` must exit nonzero while brand clearance, permanent-import/system-display policy, source correspondence or privacy remain pending. Tests must prove a document merely named “approved” is not automatically accepted or used to flip status. |
| P4 | Reseal `publication/manifest.json` from the approved change ledger. Run component source checks, legal resource checks, retired-asset checks, exact payload verification, fixture hashes, and full quick/release builds. | Expected: exact intended path union, unchanged original-copy evidence, no retired image hash in eligible files. The manifest itself is the sole self-hash exception. The source-component manifest must not hash the whole payload manifest and create a cycle. |
| P5 | Ask for approval of the exact **local** initial root commit once copyright/source/asset scope is verified. Brand and permanent-import policies may remain public-release blockers; do not claim a private development commit is clearance. Generate NUL-separated pathspecs from the verified manifest and inspect the exact index before commit. | Use the exact commands below. Verify the root has no parent and every indexed path/mode/blob matches the intended tree. If source/asset evidence is still blocked, do not create a full-tree seed just to satisfy frequent-commit advice. |
| P6 | After the clean root exists, commit each green D slice using its explicit file list. Before eventual publication, audit every commit reachable from the selected publication ref for retired image hashes and prohibited reference/binary paths. | `git rev-list main` is the selected history, not a mirror of all private refs. No old images in an earlier commit, no unrelated references, and no unreviewed source artifacts. No push command is included in this plan. |

Phase contract for `scripts/verify-publication.py` (implement/test each branch separately, not a global bypass):

- `pre-seed`: require unborn main, no refs/objects/indexed files/remotes/alternates; compare every eligible file with the manifest and no extras.
- `index`: read `git ls-files --stage -z`, require stage zero only, validate each staged blob's content hash and executable mode against the intended manifest (including the manifest file's separately calculated row); reject deletions/omissions/unlisted paths. Do not trust worktree bytes instead of staged bytes.
- `development`: compare the union of tracked files plus eligible untracked files with the manifest, and verify working-copy bytes; it must still work after the first commit. Reject nonzero Git command failures, rather than interpreting empty stdout as an empty tree.
- `archive`: verify exact file-only tar membership and content using the source/payload rows plus the manifest row; reject traversal, duplicate entries, links, unexpected files, or altered modes/bytes. A separate release receipt binds source-archive hash, payload-manifest hash, and actual binary artifact hashes.

Intended CLI:

```sh
python3 scripts/verify-publication.py pre-seed .
python3 scripts/verify-publication.py index .
python3 scripts/verify-publication.py development .
python3 scripts/verify-publication.py archive . "$RUN/corresponding-source.tar"
```

These are **new commands to implement in P2**, not commands that exist today. Each phase returns zero with `Publication payload verified: <phase>` only after all relevant checks; fixtures assert nonzero for every failure case.

Initial commit commands, only after approval and the new verifier is implemented:

```sh
python3 scripts/verify-publication.py pre-seed .
python3 - "$RUN/approved-paths.nul" <<'PY'
import json, pathlib, sys
root = pathlib.Path.cwd()
manifest = json.loads((root / 'publication/manifest.json').read_text())
paths = sorted({r['path'] for r in manifest['files']} | {'publication/manifest.json'})
pathlib.Path(sys.argv[1]).write_bytes(b''.join(p.encode() + b'\0' for p in paths))
PY
git add --pathspec-from-file="$RUN/approved-paths.nul" --pathspec-file-nul
python3 scripts/verify-publication.py index .
git -c user.name="${OWNER_NAME:?Set the owner-approved commit name}" \
    -c user.email="${OWNER_EMAIL:?Set the owner-approved commit email}" \
    commit -m "chore: seed reviewed native player baseline"
git rev-list --parents -n 1 HEAD
```

Expected final line: one root commit ID with no parent IDs. The isolated HOME deliberately does not inherit an assumed Git author; owner identity is explicit. Do not execute commits in this planning turn.


##### D — Discogs: preserve the feature and close every transient-data path

###### One data contract

Add the complete Appendix D types first through their tests:

- `DiscogsFetchStamp` carries wall time, sleep-inclusive monotonic seconds, and a boot-session identifier.
- `DiscogsContentEvidence` carries a positive release ID and a nonempty list of original acquisition stamps. Every contributing stamp must still be fresh. A search result plus release-details fallback may require two stamps; do not choose a new timestamp or simply pick the newest one.
- `DiscogsFreshness.maximumAge` is **five hours**, a conservative implementation margin below the six-hour requirement recorded in the audit. Both elapsed clocks must be finite, nonnegative and strictly below the cap. Changed boot or unavailable trusted clock means refetch, not stale reuse.
- Capture a stamp immediately before the actual uncached HTTP request, after token acquisition. Preserve it through copies, caches, thumbnail downloads, suggestions, actor hops, and mutations. An image download or cache write does not refresh the metadata's acquisition time.
- Add mandatory `fetchedAt` fields to `DiscogsArtworkCandidate`, `DiscogsSearchPage`, and `DiscogsReleaseMetadata`; add a canonical release-page URL to release metadata. Do not supply `Date()` defaults that silently make old/mock content fresh. Update each constructor/fixture deliberately and inject its clock for tests.
- Add optional `discogsEvidence` to `AudioCDMetadataCandidate`, `AudioDisc`, and `LibraryArtworkChange`; nil is ordinary non-Discogs/local data, **not permission to erase evidence from Discogs-origin callers**. Old Discogs cache entries lacking evidence are invalid; ordinary MusicBrainz entries remain ordinary data.
- Add only runtime `@Transient` evidence/fallback properties to the current `Sources/Library/Track.swift` model. Do not add stored SwiftData columns or modify historical schema definitions to guess provenance of existing saved data.

###### Acquisition/cache/attribution slices

| Task | Exact files | RED case, GREEN change, exact focused command |
|---|---|---|
| D1 | `Tests/SongbirdTests/DiscogsArtworkSearchCacheTests.swift`, `Sources/Discogs/DiscogsArtworkSearchCache.swift` | Add Appendix D's future-date regression using the existing API. RED: it returns an empty array instead of nil. GREEN: require age >= 0. `quick --filter 'DiscogsArtworkSearchCacheTests.testFutureDatedCacheEntryIsRejected'`. Commit only these two paths after clean root. |
| D2 | Same two files | Add exact-boundary regression. RED: the current <= comparison accepts age == maximum. GREEN: use strict `<`. `quick --filter 'DiscogsArtworkSearchCacheTests.testCacheExpiresAtBoundary'`. Preserve these assertions when D5 changes the API. |
| D3 | New `Tests/SongbirdTests/DiscogsClientTests.swift`; `Sources/Discogs/DiscogsClient.swift` | Use Appendix D's actual-client URLProtocol test: absolute/relative/missing/unrelated resource_url values all produce `https://www.discogs.com/release/42`; nonpositive IDs are excluded. GREEN: replace the malformed expression with the canonical positive-ID URL. `quick --filter DiscogsClientTests`. Do not claim a helper-only test proves production mapping. |
| D4 | New `Sources/Discogs/DiscogsFreshness.swift`, `Tests/SongbirdTests/DiscogsFreshnessTests.swift` | Add boundary, rollback, sleep/monotonic expiry, reboot, nil clock, empty evidence and mixed-age evidence tests from Appendix D, one assertion scenario per RED/GREEN slice. `quick --filter DiscogsFreshnessTests`. Clock sampling stays off audio callbacks and row rendering. |
| D5 | `Sources/Discogs/DiscogsTypes.swift`, `Sources/Discogs/DiscogsClient.swift`, `Sources/Discogs/DiscogsArtworkSearchCache.swift`, all their constructor test fixtures | Add mandatory acquisition stamps, a version-2 cache envelope, and a stamped cache hit. Preserve the original response stamp for empty results and sliced first-five results. Tests: old JSON is rejected, round trip does not renew age, long configured TTL is capped, future/changed-boot/expired entries are pruned, and a failed refresh never returns stale data. `quick --filter 'DiscogsArtworkSearchCacheTests|DiscogsFreshnessTests|DiscogsTypesTests'`. Use the complete reusable cache kernel in Appendix D; wire search and CD wrappers to it instead of duplicating validity rules. |
| D6 | `Sources/Discogs/DiscogsClient.swift`, new `Sources/Discogs/DiscogsHTTP.swift`, `Tests/SongbirdTests/DiscogsClientTests.swift` | Real-client fixture asserts an ephemeral/no-URLCache default session, `.reloadIgnoringLocalCacheData`, and `Cache-Control: no-cache` on search/release/image requests. Capture original stamp before request; reject responses that arrive after expiry. `quick --filter DiscogsClientTests`. Preserve token/error/Retry-After handling; do not expose tokens in tests/logs. |
| D7 | `Sources/Discogs/DiscogsClient.swift`, new `Tests/SongbirdTests/DiscogsDownloadFreshnessTests.swift` | Change the Discogs image method to `downloadImage(from:evidence:)`; check before each attempt, after every await/backoff, and before returning bytes. Add `.resultsExpired` and `.freshnessUnavailable` to DiscogsError with exact user-facing messages. Test a fake delayed response crossing expiry, expiry during retry, and already-expired input causing zero requests. `quick --filter DiscogsDownloadFreshnessTests`. Update all production naked-URL calls; there must be no expiry-unchecked overload used by Discogs. |
| D8 | New `Sources/Discogs/DiscogsAttributionView.swift`; `Sources/Discogs/DiscogsSearchView.swift` | Add the Appendix D link as a **sibling** of the existing selection Button, adjacent to title/artist/year/image. Do not nest Link inside Button. Preserve row selection, labels and layout; link activation must not select, download or apply. `quick --filter 'DiscogsClientTests|DiscogsArtworkTargetStabilityTests'`; V2 tests the rendered interaction separately. |
| D9 | New `Sources/Discogs/DiscogsFreshnessView.swift`; `Sources/Discogs/DiscogsSearchView.swift`; new `Tests/SongbirdTests/DiscogsReviewFreshnessTests.swift` | One freshness wrapper per result surface (Appendix D), not a timer per row. On tick/wake/activation, clear expired candidates/selection, cancel downloadTask, change downloadGeneration, retain local search fields/album targets, and show `Discogs results expired. Search again.` Guard post-search publication and post-download submission too. Tests use a controllable clock and delayed result seam; expired selection never invokes applyArtworkChanges. `quick --filter DiscogsReviewFreshnessTests`. |
| D10 | `Sources/Discogs/DiscogsBulkReviewView.swift`, `Sources/Discogs/DiscogsArtworkSuggestionPolicy.swift`, `Tests/SongbirdTests/DiscogsReviewFreshnessTests.swift` | Preserve evidence in every review/retry suggestion; filter expired early results before publishing the completed batch. On expiry remove stale candidates, clamp reviewIndex, cancel downloads/invalidate generation, and require explicit re-search/review. Do not silently promote a different release as the old reviewed selection. Add sibling attribution to each candidate. Test a first result expiring while later album searches run. `quick --filter 'DiscogsReviewFreshnessTests|DiscogsArtworkSuggestionPolicyTests'`. |
| D11 | `Sources/Discogs/DiscogsBulkReviewView.swift`, `Tests/SongbirdTests/DiscogsDownloadFreshnessTests.swift` | In `applyCandidate` and `applyArtworkSuggestions`, preserve evidence through normalization and `LibraryArtworkChange`; validate the complete downloaded batch again after the last download. Test the first image expiring while the last image downloads. Expected: no batch submission/partial persistence. `quick --filter DiscogsDownloadFreshnessTests`. |

D5 changes cache consumers in exactly these existing places: `DiscogsBulkReviewView.performBulkSearch`, `DiscogsGenreReviewView.performBulkSearch`, `MissingYearView.performSearch`, `DiscogsAudioCDMetadataProvider.candidates`, and `RealLibraryRemediator`'s Discogs helpers. A stamped hit exposes `.value` (the candidate array) and `.fetches`; storing requires the original fetched stamp(s), not a newly sampled store time. Negative search results use the page stamp even when there is no candidate. Existing fixtures that only exercise value policies may use a fresh sample; freshness tests must use a private injected fake clock, never global mutable test state.

###### Mutation, year/genre, CD, thumbnail, and maintenance slices

| Task | Exact files | RED case, GREEN change, exact focused command |
|---|---|---|
| D12 | `Sources/Library/LibraryHealthMutationService.swift`, `Tests/SongbirdTests/LibraryHealthMutationTests.swift` | Add optional evidence to LibraryArtworkChange's explicit initializer with default nil; add `.remoteEvidenceExpired`. Check every remote change before normalization, immediately before assignment, and before save, with a fresh injected clock sample at each boundary. Preserve typed expiry errors through rollback rather than wrapping them as `.persistence`. Test one expired item rejects a whole batch; crossing expiry during normalization causes no saved album change; nil-evidence local art and existing Undo remain unchanged. `quick --filter LibraryHealthMutationTests`. Appendix D supplies the exact guard and replacement initializer. |
| D13 | `Sources/Discogs/DiscogsGenreReviewView.swift`, `Sources/Discogs/DiscogsGenreBulkApplier.swift`, `Sources/Views/LibraryItemActionHandler.swift`, `Sources/Views/MissingGenreView.swift`, `Sources/Views/SettingsView.swift`, `Tests/SongbirdTests/DiscogsGenreSuggestionPolicyTests.swift` | Add evidence to private DiscogsGenreCandidate and DiscogsGenreAlbumSuggestion. Add adjacent attribution. Route single and bulk genre changes through the existing bulk applier via the shared action handler, rather than keeping a view-owned independent save. Check all evidence before changes and immediately before save; preserve missing-only semantics, casing, duplicate/removed-album behavior and rollback. `quick --filter DiscogsGenreSuggestionPolicyTests`. Split presenter wiring into one file per green edit. |
| D14 | `Sources/Views/MissingYearView.swift`, new `Sources/Discogs/DiscogsYearApplier.swift`, `Sources/Views/LibraryItemActionHandler.swift`, new `Tests/SongbirdTests/DiscogsYearFreshnessTests.swift` | Replace SearchResult's retained live Album with `DiscogsArtworkAlbumTarget`; retain candidate evidence and canonical source URL instead of only `"cached"`/`"discogs"`. Add adjacent attribution and loaded-state expiry. Implement missing-only single/bulk year application through one stable-ID action boundary, with all-evidence pre/save checks and rollback. Test deleted/replaced targets, already-filled year, cache-origin attribution, and expiry causing zero save. `quick --filter DiscogsYearFreshnessTests`. |
| D15 | `Sources/OpticalDisc/MusicBrainzCDMetadata.swift`, `Sources/OpticalDisc/DiscogsCDMetadata.swift`, `Tests/SongbirdTests/AudioCDFeatureTests.swift` | Keep MusicBrainz's existing generic cache policy. Add explicitly named Discogs load/store methods using the shared stamped cache kernel, and call them from DiscogsAudioCDMetadataProvider even if a caller injected a generic long-lifetime cache. Reject old Discogs entries without evidence. Combine search and release stamps if fallback artwork came from the search. Test a deliberately injected 30-day cache cannot bypass expiry; no-CD-Text does not store an empty result that masks a later usable query; fresh cache still avoids duplicate requests. `quick --filter AudioCDFeatureTests`. The composite MusicBrainz/Discogs provider has no extra cache—do not invent one. |
| D16 | `Sources/OpticalDisc/OpticalDiscService.swift`, `Sources/OpticalDisc/AudioDisc.swift`, new `Tests/SongbirdTests/OpticalDiscMetadataFreshnessTests.swift` | Add `startsObserving: Bool = true` and clock seams to the existing service initializer for deterministic tests. Preserve original CD-Text/default metadata per disc ID. Add refresh-generation checks after await, clear old candidate arrays on empty refresh/removal, reject expired selections, and schedule expiry while metadata is owned even when AudioCDView is closed. On expiry restore fallback metadata while preserving sectors/device/disc identity/status/playback. `quick --filter OpticalDiscMetadataFreshnessTests`. Fake access/eject/metadata providers only; no physical drive. |
| D17 | `Sources/Library/Track.swift`, `Sources/OpticalDisc/AudioDisc.swift`, `Sources/Views/AudioCDView.swift`, new `Tests/SongbirdTests/DiscogsTransientPresentationTests.swift` | Add runtime-only evidence and original title/artist/album fallback data to transient CD tracks; pass them through `Track.transientAudioCDTrack` at both AudioCDView call sites. Never infer provider by CDN hostname. Add attributed CD header/candidate presentation; replace the button-only metadata dialog with a small sheet only if a sibling link cannot be represented there. Test expiry drops remote metadata but preserves track ID, source sectors, queue occurrence and playback controls. `quick --filter DiscogsTransientPresentationTests`. No stored-column migration. |
| D18 | `Sources/Library/LibrarySnapshot.swift`, `Sources/Library/ArtworkThumbnailService.swift`, `Tests/SongbirdTests/ArtworkRefreshTests.swift` | Add `.discogsRemote(URL, evidence: DiscogsContentEvidence)` to ArtworkReference; include evidence in its hash/cache key. Use an uncached Discogs-specific image loader. Check freshness before a cached return, after joined/in-flight completion, and before insertion/publication; prune expired Discogs entries and cancel expired in-flight loads. Do not change ordinary local `.album`, `.embedded`, or generic MusicBrainz `.remote` behavior. `quick --filter 'ArtworkRefreshTests|ArtworkThumbnailServiceTests'`. |
| D19 | `Sources/Views/ArtworkThumbnailView.swift`, `Sources/ServicePane/ServicePaneView.swift`, `Sources/Views/NowPlayingBar.swift`, `Sources/Views/MiniPlayerView.swift`, `Tests/SongbirdTests/DiscogsTransientPresentationTests.swift` | Propagate the provider-aware reference at every CD image path, including the sidebar's direct AudioDisc URL construction. Clear an already-loaded CGImage when evidence expires; a cache eviction alone is insufficient. Show an adjacent source link outside the image's fixed frame. Keep one expiry owner per surface, not per image pixel/row. Test loaded-image expiry and reference changes without affecting local artwork. `quick --filter 'DiscogsTransientPresentationTests|ArtworkRefreshTests'`. |
| D20 | `Sources/Views/PlayQueueView.swift`, `Sources/Views/NowPlayingPaneView.swift`, `Sources/Views/NowPlayingBar.swift`, `Sources/Views/MiniPlayerView.swift`, `Sources/Playback/NowPlayingCommandCenter.swift`, `Tests/SongbirdTests/DiscogsTransientPresentationTests.swift` | Consume the shared transient presentation/fallback values so copied CD strings do not outlive evidence in queue/history/player surfaces. System Now Playing cannot be assumed to satisfy a clickable-source-link requirement; implement/test clearing stale system artwork on same-track expiry, but do not choose a fresh Discogs system-display policy silently. That policy remains gate G-DISPLAY; approved conservative fallback uses original CD-Text/default metadata and no Discogs artwork. Do not add unsupported attribution metadata fields. `quick --filter DiscogsTransientPresentationTests`; playback/backend regression filters too. |
| D21 | `Sources/OpticalDisc/AudioCDRipCoordinator.swift`, `Sources/OpticalDisc/AudioCDRipper.swift`, `Tests/SongbirdTests/AudioCDRipperTests.swift` | Guard Discogs evidence before filename/metadata use and after long ripping/download/import awaits. Stage derived values before persistent assignments. If the captured source expires, do not import stale metadata/artwork; retain completed audio files and surface a recoverable refresh/reconfirmation choice or an explicitly selected fallback. Do not silently rename/delete completed files. Test a controlled long rip crossing expiry. `quick --filter AudioCDRipperTests`. G-IMPORT now preserves successfully saved metadata, filenames and history; new imports still require fresh evidence. |
| D22 | `Sources/Library/RealLibraryRemediator.swift`, `Tools/SongbirdLibraryRemediator/main.swift`, `Tests/SongbirdTests/RealLibraryRemediatorTests.swift` | Add internal injected client/cache/clock seams. In `applyDiscogsEvidence`, finish fetching/download/normalization before any genre/year/artwork mutation; validate after the final await and before save. In `applyDiscogsTrackNumbers`, preserve search and release evidence across throttling and recheck before assignments/save. Add source link/freshness to transient audit output; saved reports remain historical records under the nonblocking G-IMPORT decision. Test an expired download leaves all album fields unchanged. `quick --filter RealLibraryRemediatorTests`. Never execute maintenance against the normal store. |
| D23 | All D production/test paths; `docs/DISCOGS_CONTENT_POLICY.md`, `NEXT_STEPS.md`, `CONTINUITY.md` | Check every call to downloadImage and every search/cache consumer for evidence loss. Document technical behavior separately from unresolved saved-content/system-display permission. Do not backfill provenance from existing artwork bytes, dates or guessed URLs. | `quick --filter 'Discogs|LibraryHealthMutationTests|AudioCDFeatureTests|OpticalDiscMetadataFreshnessTests|ArtworkRefreshTests|ArtworkThumbnailServiceTests|AudioCDRipperTests|RealLibraryRemediatorTests'`; require nonzero named coverage and no skipped failing suite. |
| D24 | `Tests/SongbirdTests/DiscogsReviewFreshnessTests.swift`, `Tests/SongbirdTests/DiscogsTransientPresentationTests.swift`, relevant new helpers | Add final mixed-path regression: cached hit → displayed result → delayed image → actor hop → expiry. Assert no stale save, no stale image/state republish, preserved local data, and explicit re-search requirement. | Full `quick`, then optimized release build. When playback/CD/render-adjacent paths or vendored libraries changed, run isolated audio TSan too; never claim hardware acceptance from these tests. |

###### Exact mutation insertion contract

In `LibraryHealthMutationService.applyArtwork`, call the Appendix D `requireFreshDiscogsEvidence` guard on `changes.compactMap(\.discogsEvidence)` before the initial preparation loop, again after `after` is assembled and **before** assigning album data, and again immediately before `modelContext.save()`. Take the clock sample inside that actor/function, not at the caller before an actor queue wait. Use a per-call injectable clock closure for tests; never a mutable global override. Add this typed catch after cancellation handling and before the generic persistence catch:

```swift
} catch let error as LibraryHealthMutationError {
    modelContext.rollback()
    throw error
}
```

The existing `applyArtworkChanges` closure type does not need to change. Discogs views provide `LibraryArtworkChange(..., discogsEvidence: candidate.evidence)`; local/user image callers omit the optional parameter and retain their current semantics. Existing Undo receipts must not be expired/deleted as if they were a disposable API response cache. The owner retention decisions preserve existing Undo semantics without response-cache expiry or migration.

###### Exact UI expiry rules

Implement per-view `expireResults(at:)` methods, called from the common Appendix D wrapper's tick callback, not during `body` evaluation:

- Manual `DiscogsSearchView`: retain searchTitle/searchArtist/searchYear and albumTargets; remove expired candidates, clear expired selectedCandidate, cancel downloadTask, replace downloadGeneration, set `downloadError` to the exact expiry message. Do not let a cancelled completion republish data or success.
- `DiscogsBulkReviewView`: recreate immutable ReviewItem values with fresh candidates only; remove empty items, clamp reviewIndex, prune failedArtworkSuggestions, cancel/invalidate downloads, and require re-search if the current review expired. Validate local `items` immediately before assigning reviewItems after a long search.
- `DiscogsGenreReviewView`: preserve evidence in DiscogsGenreCandidate, purge/clamp review state, cancel applyTask and replace applyGeneration, set applyError, and rerun the existing query only on explicit user action.
- `MissingYearView`: preserve local album targets, remove expired SearchResult values, invalidate the search task's old publication generation as needed, and return to an explicit lookup state without deleting stored years.
- CD service: owns its own expiry task and wake/activation checks; it is not contingent on any particular window remaining open. Restoring fallback must preserve audio source identifiers and playback positions. A timer controls presentation cleanup; the final mutation guard is always independent.

###### Human policy gates: do not silently “solve” them

Maintain `docs/DISCOGS_CONTENT_POLICY.md` with the current owner decisions, terms
reference/date and affected code paths. The original pending retention instructions
are superseded by the bound nonblocking decisions recorded above:

- G-ARTWORK: selected saved covers remain permanent, including offline rendering and Undo; owner-nonblocking.
- G-IMPORT: imported metadata, saved CD filenames, maintenance reports and metadata Undo remain until the user changes/removes them; independently owner-nonblocking. No schema or historic-library rewrite is needed or authorized.
- G-DISPLAY: original CD-Text/default metadata and no transient Discogs artwork in system Now Playing are approved; implementation and acceptance remain pending. Do not claim adding a URL to an unrelated metadata field satisfies attribution.

Preserve Discogs importing and all saved covers/tags/reports/Undo. Keep owner
release decisions distinct from verified provider permission. Any future stored
provenance migration requires a separate approved ExecPlan.

##### Artwork design contract — do not improvise

Use an original, flat geometric record motif, not a bird, silhouette traced from existing art, artist portrait, stock image, font glyph, SF Symbol export, or recreated historical Songbird logo. The default Dock tile is graphite with a blue center label; themed variants change only restrained background/accent colors. No gradients, textures, glow, bevels, characters, extra typography, or ornamental badges.

Keep all existing resource basenames and preference mappings. New PNGs are 1024×1024 RGBA in sRGB-compatible rendering. Logo aliases contain the same monochrome record mark on a transparent canvas; the missing-art tile uses a neutral record/sleeve geometry. The SVG is a generic independently authored disc outline. Generation reads project source/specification only, never an old image. Do not claim prior exposure to historical assets was a certified clean-room process.

###### A1 — Fail on absent provenance and retired runtime artwork

Files: add `Tests/SongbirdTests/ArtworkProvenanceTests.swift`; later add `Sources/Resources/asset-provenance.json` and `publication/retired-asset-hashes.json`.

First record the SHA-256 values (not image bytes) of the current runtime PNG/SVG files and four screenshots into the retired-hash record before replacing them. The old publication manifest supplies those exact before-hashes. Add the verified legacy branding hash `bcf5041f86decc7e4de45c0258a72f3b727fb27a76dd11988d03383e15709105` as an extra forbidden legacy image. This record is evidence, not a copy of the artwork or a license.

Add this test file before implementing the generator:

```swift
import CryptoKit
import Foundation
import XCTest
@testable import SongbirdLib

final class ArtworkProvenanceTests: XCTestCase {
    private var root: URL {
        URL(fileURLWithPath: #filePath)
            .deletingLastPathComponent()
            .deletingLastPathComponent()
            .deletingLastPathComponent()
    }

    private struct Manifest: Decodable {
        let schema: Int
        let method: String
        let license: String
        let files: [Entry]
    }

    private struct Entry: Decodable {
        let name: String
        let sha256: String
    }

    func testEveryRuntimeImageHasOriginalGenerationEvidence() throws {
        let folder = root.appendingPathComponent("Sources/Resources")
        let manifestURL = folder.appendingPathComponent("asset-provenance.json")
        XCTAssertTrue(FileManager.default.fileExists(atPath: manifestURL.path),
                      "Runtime images need original-generation provenance")
        guard FileManager.default.fileExists(atPath: manifestURL.path) else { return }
        let manifest = try JSONDecoder().decode(Manifest.self, from: Data(contentsOf: manifestURL))
        XCTAssertEqual(manifest.schema, 1)
        XCTAssertEqual(manifest.method, "project-authored-geometric-renderer")
        XCTAssertEqual(manifest.license, "GPL-3.0-or-later")
        let actual = try FileManager.default.contentsOfDirectory(
            at: folder, includingPropertiesForKeys: nil
        ).filter { ["png", "svg"].contains($0.pathExtension) }
        XCTAssertEqual(Set(actual.map(\.lastPathComponent)), Set(manifest.files.map(\.name)))
        XCTAssertEqual(manifest.files.count, Set(manifest.files.map(\.name)).count)
        for entry in manifest.files {
            let data = try Data(contentsOf: folder.appendingPathComponent(entry.name))
            let digest = SHA256.hash(data: data).map { String(format: "%02x", $0) }.joined()
            XCTAssertEqual(digest, entry.sha256, entry.name)
        }
        let dockNames = Set(SongbirdDockIconChoice.allCases.map { $0.resourceName + ".png" })
        XCTAssertTrue(dockNames.isSubset(of: Set(manifest.files.map(\.name))))
    }
}
```

RED command: `quick --filter ArtworkProvenanceTests`. Expected: `testEveryRuntimeImageHasOriginalGenerationEvidence` fails with the provenance message, not a build error. Keep test input media synthetic.

###### A2 — Establish one shared catalog of original icon variants

Files: add `Sources/DockIconSupport/SongbirdArtworkCatalog.swift`; add `scripts/generate-original-artwork.swift` as a standalone generator using that catalog, not a new SwiftPM executable product. `SongbirdDockIconSupport` is intentionally independent of SongbirdLib so the retained Dock never loads persistence/playback/integrations.

Use this exact catalog shape and resource/color list. Hex strings are RGB, with no `#` prefix:

```swift
import Foundation

public enum SongbirdArtworkCatalog {
    public struct Variant: Sendable {
        public let name: String
        public let background: UInt32
        public let accent: UInt32
    }

    public static let dockVariants: [Variant] = [
        .init(name: "dock-icon-blue-outline", background: 0x202831, accent: 0x58B9E8),
        .init(name: "dock-icon-blue-vinyl", background: 0x1C2635, accent: 0x80C8EC),
        .init(name: "dock-icon-gonzo-light", background: 0xE9E0CD, accent: 0xA88030),
        .init(name: "dock-icon-gonzo-dark", background: 0x302B24, accent: 0xC49A45),
        .init(name: "dock-icon-pink-lacquer", background: 0xEEDDE1, accent: 0xAD6179),
        .init(name: "dock-icon-pink-burgundy", background: 0x39252E, accent: 0xCC829B),
        .init(name: "dock-icon-pink-terrazzo", background: 0xDDD0D5, accent: 0x945A75),
        .init(name: "dock-icon-bowie-silver-blue", background: 0xCED6DF, accent: 0x587EA8),
        .init(name: "dock-icon-bowie-prism", background: 0x28333E, accent: 0x9BA8CE),
        .init(name: "dock-icon-dove-pearl", background: 0xE8E5DE, accent: 0x8D918C),
        .init(name: "dock-icon-dove-marble", background: 0xD1D4D2, accent: 0x777F7A),
        .init(name: "dock-icon-silverwing-frame", background: 0xC5CCD3, accent: 0x627B92),
        .init(name: "dock-icon-silverwing-graphite", background: 0x282D32, accent: 0xA0ABB4),
        .init(name: "dock-icon-music-light-lacquer", background: 0xECE5E3, accent: 0xB15E58),
        .init(name: "dock-icon-music-dark-red", background: 0x302426, accent: 0xD37872),
        .init(name: "dock-icon-purple-rim", background: 0x302839, accent: 0xAC87CB),
        .init(name: "dock-icon-purple-neon", background: 0x27232F, accent: 0xBD9AD9),
        .init(name: "dock-icon-nightingale-glow", background: 0x263638, accent: 0x89C1BC),
        .init(name: "dock-icon-nightingale-glass", background: 0xCEDFDF, accent: 0x5B969C),
        .init(name: "dock-icon-blackbird-graphite", background: 0x25282C, accent: 0xAAB0B8),
        .init(name: "dock-icon-blackbird-amber", background: 0x2D2925, accent: 0xD6974D),
    ]

    public static let resourceNames = Set(dockVariants.map(\.name))
}
```

The catalog contains no image data and uses current basenames only as compatibility identifiers. This does not clear product/theme names; branding remains a gate.

###### A3 — Generate artwork from complete source, not from old PNGs

Implement the full generator in the original-artwork appendix below. Compile only these two source files:

```sh
swiftc -parse-as-library \
  Sources/DockIconSupport/SongbirdArtworkCatalog.swift \
  scripts/generate-original-artwork.swift \
  -framework AppKit -o "$RUN/generate-original-artwork"
mkdir -p "$RUN/art-first" "$RUN/art-second"
"$RUN/generate-original-artwork" "$RUN/art-first"
"$RUN/generate-original-artwork" "$RUN/art-second"
diff -qr "$RUN/art-first" "$RUN/art-second"
```

Expected: two complete sets of 27 images plus provenance; no differences within the same toolchain. Cross-toolchain PNG encoder differences require regenerated hashes and review, not fabricated byte identity. Copy only the named generated image files and their provenance into `Sources/Resources/`; preserve unrelated license resources.

GREEN command: `quick --filter ArtworkProvenanceTests`. Then `quick --filter 'ThemeTests|ArtworkThumbnailServiceTests'`. Expected: actual named suites pass, including square missing-art and lazy packaged-resource behavior. Do not remove tests to hide an image-format or alpha error.

###### A4 — Remove obsolete Dock crops without changing persisted choices

Files: `Sources/DockIconSupport/SongbirdDockIconArtwork.swift`, `Sources/Utils/SongbirdDockIconChoice.swift`, `Tests/SongbirdTests/ThemeTests.swift`.

Add a test of the existing public `dockReadyImage(from:resourceName:)` using a synthetic four-quadrant 1024-pixel image and two different resource names; the rendered pixel buffers must match. RED: current per-image crops yield different pixels. GREEN: replace the source crop calculation with `NSRect(origin: .zero, size: sourceImage.size)` and validate names against `SongbirdArtworkCatalog.resourceNames`; remove the private SourceCrop type, sourceRect helper, and old crop dictionary. Keep canvasDimension, opticalTileDimension, edgeCleanupInset, clipping, and rendering unchanged.

Change only `.terminalAmber`'s display label from `Amber Bird` to `Amber Record`; retain raw values and resource aliases. Add a label assertion. Run `quick --filter ThemeTests`; expected: choice scoping/persistence, retained-Dock validation, and `testDockIconResourcesLoad` still pass. Its alpha expectations are transparent at `(50,512)` and opaque at `(512,512)` after the Dock renderer.

###### A5 — Retire old screenshots before the first clean commit

Files: `README.md`, `docs/screenshots/README.md`, `docs/screenshots/manifest.json`, and exactly the four existing screenshot PNGs named by that manifest.

Remove the old PNGs from the candidate and their README image references; do not place the old bytes under another tracked archive directory. Set screenshot metadata to a clearly pending recapture state and replace the gallery with a short factual note. Update the screenshot notes so they no longer claim old captures show the replacement assets. Keep the before-hashes in `publication/retired-asset-hashes.json`.

Fresh screenshots are a later explicit disposable-harness task, not permission to launch the installed app or invent screenshots. Use `docs/screenshots/README.md`'s existing journey only after a fresh package contains the replacements and required local permissions are available. In this signing-excluded plan, absence of new marketing screenshots is acceptable; retention of uncleared old screenshots is not. Validate all README/local links after removal.

#### Appendix: complete original-artwork generator

Add as `scripts/generate-original-artwork.swift` during GREEN, not during planning:

```swift
import AppKit
import CryptoKit
import Foundation

@main
struct GenerateOriginalArtwork {
    private struct Entry: Codable { let name: String; let sha256: String }
    private struct Manifest: Codable {
        let schema: Int
        let method: String
        let license: String
        let generator: String
        let catalog: String
        let files: [Entry]
    }

    @MainActor
    static func main() throws {
        guard CommandLine.arguments.count == 2 else {
            throw NSError(domain: "ArtworkGenerator", code: 2, userInfo: [
                NSLocalizedDescriptionKey: "Usage: generate-original-artwork OUTPUT_DIRECTORY"
            ])
        }
        let output = URL(fileURLWithPath: CommandLine.arguments[1], isDirectory: true)
        try FileManager.default.createDirectory(at: output, withIntermediateDirectories: true)
        var assets: [String: Data] = [:]
        for variant in SongbirdArtworkCatalog.dockVariants {
            assets[variant.name + ".png"] = try render(
                background: color(variant.background), accent: color(variant.accent), logo: false
            )
        }
        assets["dock-icon.png"] = assets["dock-icon-blue-outline.png"]!
        let logo = try render(background: .clear, accent: .black, logo: true)
        for name in ["songbird-logo.png", "logo.png", "transparent.png"] { assets[name] = logo }
        assets["missing-album-artwork.png"] = try render(
            background: color(0xD8DADD), accent: color(0x939BA3), logo: false
        )
        let svg = """
        <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32">
          <g fill="none" stroke="currentColor" stroke-width="1.5">
            <circle cx="16" cy="16" r="13"/>
            <circle cx="16" cy="16" r="4"/>
            <circle cx="16" cy="16" r="1"/>
          </g>
        </svg>
        """
        assets["audio-cd-mark.svg"] = Data((svg + "\n").utf8)
        var entries: [Entry] = []
        for name in assets.keys.sorted() {
            let data = assets[name]!
            let destination = output.appendingPathComponent(name)
            guard !FileManager.default.fileExists(atPath: destination.path) else {
                throw NSError(domain: "ArtworkGenerator", code: 3, userInfo: [
                    NSLocalizedDescriptionKey: "Refusing to overwrite " + name
                ])
            }
            try data.write(to: destination, options: .atomic)
            entries.append(Entry(name: name, sha256: SHA256.hash(data: data)
                .map { String(format: "%02x", $0) }.joined()))
        }
        let manifest = Manifest(
            schema: 1, method: "project-authored-geometric-renderer",
            license: "GPL-3.0-or-later", generator: "scripts/generate-original-artwork.swift",
            catalog: "Sources/DockIconSupport/SongbirdArtworkCatalog.swift", files: entries
        )
        let encoder = JSONEncoder()
        encoder.outputFormatting = [.prettyPrinted, .sortedKeys, .withoutEscapingSlashes]
        try encoder.encode(manifest).write(to: output.appendingPathComponent("asset-provenance.json"))
        print("Generated \(entries.count) original geometric images and provenance")
    }

    private static func color(_ hex: UInt32) -> NSColor {
        NSColor(srgbRed: CGFloat((hex >> 16) & 255) / 255,
                green: CGFloat((hex >> 8) & 255) / 255,
                blue: CGFloat(hex & 255) / 255, alpha: 1)
    }

    @MainActor
    private static func render(background: NSColor, accent: NSColor, logo: Bool) throws -> Data {
        guard let bitmap = NSBitmapImageRep(
            bitmapDataPlanes: nil, pixelsWide: 1024, pixelsHigh: 1024,
            bitsPerSample: 8, samplesPerPixel: 4, hasAlpha: true,
            isPlanar: false, colorSpaceName: .deviceRGB, bytesPerRow: 0, bitsPerPixel: 0
        ), let context = NSGraphicsContext(bitmapImageRep: bitmap) else {
            throw NSError(domain: "ArtworkGenerator", code: 4)
        }
        NSGraphicsContext.saveGraphicsState()
        defer { NSGraphicsContext.restoreGraphicsState() }
        NSGraphicsContext.current = context
        context.shouldAntialias = true
        context.cgContext.clear(CGRect(x: 0, y: 0, width: 1024, height: 1024))
        if !logo {
            background.setFill()
            NSBezierPath(roundedRect: NSRect(x: 0, y: 0, width: 1024, height: 1024),
                         xRadius: 180, yRadius: 180).fill()
        }
        (logo ? NSColor.black : NSColor(white: 0.12, alpha: 1)).setFill()
        NSBezierPath(ovalIn: NSRect(x: 184, y: 184, width: 656, height: 656)).fill()
        for radius in [CGFloat(224), CGFloat(264), CGFloat(304)] {
            let groove = NSBezierPath(ovalIn: NSRect(
                x: 512 - radius, y: 512 - radius, width: radius * 2, height: radius * 2
            ))
            groove.lineWidth = logo ? 14 : 6
            if logo {
                context.cgContext.setBlendMode(.clear)
                NSColor.black.setStroke()
                groove.stroke()
                context.cgContext.setBlendMode(.normal)
            } else {
                NSColor(white: 0.28, alpha: 1).setStroke()
                groove.stroke()
            }
        }
        if logo {
            context.cgContext.setBlendMode(.clear)
            NSColor.black.setFill()
            NSBezierPath(ovalIn: NSRect(x: 480, y: 480, width: 64, height: 64)).fill()
            context.cgContext.setBlendMode(.normal)
        } else {
            accent.setFill()
            NSBezierPath(ovalIn: NSRect(x: 414, y: 414, width: 196, height: 196)).fill()
            background.setFill()
            NSBezierPath(ovalIn: NSRect(x: 494, y: 494, width: 36, height: 36)).fill()
        }
        guard let data = bitmap.representation(using: .png, properties: [:]) else {
            throw NSError(domain: "ArtworkGenerator", code: 5)
        }
        return data
    }
}
```

The generator uses `@MainActor` AppKit rendering and an isolated entry point. No network, fonts, existing PNG inputs, random numbers, timestamps, or external artwork are involved. The renderer code and catalog are the preferred form for modifying these generated images; retain them with source distribution.

##### A4 test body to add inside ThemeTests

```swift
    @MainActor
    func testStandardizedArtworkDoesNotUseLegacyPerFileCrops() throws {
        let bitmap = try XCTUnwrap(NSBitmapImageRep(
            bitmapDataPlanes: nil, pixelsWide: 1024, pixelsHigh: 1024,
            bitsPerSample: 8, samplesPerPixel: 4, hasAlpha: true,
            isPlanar: false, colorSpaceName: .deviceRGB, bytesPerRow: 0, bitsPerPixel: 0
        ))
        bitmap.size = NSSize(width: 1024, height: 1024)
        let context = try XCTUnwrap(NSGraphicsContext(bitmapImageRep: bitmap))
        NSGraphicsContext.saveGraphicsState()
        NSGraphicsContext.current = context
        let colors: [NSColor] = [.red, .green, .blue, .yellow]
        for index in 0..<4 {
            colors[index].setFill()
            NSBezierPath(rect: NSRect(x: CGFloat(index % 2) * 512,
                                      y: CGFloat(index / 2) * 512,
                                      width: 512, height: 512)).fill()
        }
        NSGraphicsContext.restoreGraphicsState()
        let source = NSImage(size: NSSize(width: 1024, height: 1024))
        source.addRepresentation(bitmap)
        func pixels(_ name: String) throws -> Data {
            let rendered = SongbirdDockIconArtwork.dockReadyImage(from: source, resourceName: name)
            let tiff = try XCTUnwrap(rendered.tiffRepresentation)
            let rep = try XCTUnwrap(NSBitmapImageRep(data: tiff))
            let bytes = try XCTUnwrap(rep.bitmapData)
            return Data(bytes: bytes, count: rep.bytesPerRow * rep.pixelsHigh)
        }
        XCTAssertEqual(try pixels("dock-icon-blue-outline"),
                       try pixels("dock-icon-silverwing-frame"))
    }

    func testTerminalLegacyChoiceUsesNeutralArtworkLabel() {
        XCTAssertEqual(SongbirdDockIconChoice.terminalAmber.displayName, "Amber Record")
    }
```

Run `quick --filter 'ThemeTests.testStandardizedArtworkDoesNotUseLegacyPerFileCrops|ThemeTests.testTerminalLegacyChoiceUsesNeutralArtworkLabel'` before changing the renderer/label. Both should fail on current behavior. Then make the A4 edits and require both to pass; run all ThemeTests afterward. The pixel fixture contains no old artwork.

#### Risks, tradeoffs, and open questions

- Copyright-owner wording and contribution authority require confirmation; do not invent assignments.
- The owner approved retaining Songbird and made the naming gate nonblocking. New geometric images and owner approval do not establish independent trademark clearance for the name or existing theme labels.
- Permanent Discogs artwork/tag imports are not made lawful merely by a five/six-hour response cache. The owner selected a policy-confirmation release gate; do not implement destructive expiry of saved user content.
- Actual vendor source correspondence must be demonstrated from recorded inputs. A guessed release tag, current upstream main, or a dylib version number is not evidence.
- New images must be inspected at actual Dock, idle player, About, missing-art, and Settings sizes before making visual-quality claims. The plan deliberately limits design choices instead of authorizing a redesign.
- Existing build diagnostics and optional test skips remain baseline facts, not permission to suppress new failures.

#### Idempotence and recovery

Preserve originals in the already separate development checkout; do not add retired binary/image backups to the candidate. Use fresh disposable generation/build directories and fail on collisions. Preserve pre-change metadata/artwork fixtures only in disposable test roots, and never expire or remove user-owned album artwork in response to cache-policy tests. Record every blocked gate as pending.

#### Appendix L: complete legal-resource implementation and tests

All code in these appendices is proposed, uncompiled planning material. Use the task-level RED/GREEN order above.

##### scripts/legal_resources.py

```python
from pathlib import Path
import argparse
import shutil

LEGAL = {
    "GPL-3.0.txt": "LICENSE",
    "Songbird-NOTICE.txt": "NOTICE.txt",
    "THIRD-PARTY-NOTICES.txt": "THIRD_PARTY_NOTICES.txt",
    "CORRESPONDING-SOURCE.txt": "SOURCE_CODE.md",
}

def regular_bytes(path):
    path = Path(path)
    if any(p.is_symlink() for p in (path, *path.parents)):
        raise ValueError(f"Symlink rejected: {path}")
    if not path.is_file():
        raise ValueError(f"Missing regular file: {path}")
    data = path.read_bytes()
    if not data.decode("utf-8").strip():
        raise ValueError(f"Empty legal text: {path}")
    return data

def expected(root):
    return {
        name: regular_bytes(Path(root) / original)
        for name, original in LEGAL.items()
    }

def check(root, destination):
    destination = Path(destination)
    wanted = expected(root)
    if destination.is_symlink() or not destination.is_dir():
        raise ValueError("Missing legal directory")
    actual = {p.name for p in destination.iterdir()}
    if actual != set(wanted):
        raise ValueError("Legal file set mismatch")
    for name, data in wanted.items():
        if regular_bytes(destination / name) != data:
            raise ValueError(f"Legal text mismatch: {name}")

def stage(root, destination):
    root, destination = Path(root), Path(destination)
    source = root / "Sources/Resources/Legal"
    check(root, source)
    if destination.exists() or destination.is_symlink():
        raise ValueError("Legal destination must be new")
    if any(p.is_symlink() for p in destination.parents):
        raise ValueError("Symlink destination parent")
    destination.parent.mkdir(parents=True, exist_ok=True)
    shutil.copytree(source, destination)
    check(root, destination)

if __name__ == "__main__":
    parser = argparse.ArgumentParser()
    parser.add_argument("action", choices=("check", "stage"))
    parser.add_argument("root", type=Path)
    parser.add_argument("destination", type=Path)
    args = parser.parse_args()
    {"check": check, "stage": stage}[args.action](
        args.root, args.destination
    )
```

##### scripts/tests/test_legal_resources.py

```python
import sys
import tempfile
import unittest
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parents[1]))
from legal_resources import LEGAL, check, stage

class LegalResourceTests(unittest.TestCase):
    def setUp(self):
        self.temp = tempfile.TemporaryDirectory()
        self.addCleanup(self.temp.cleanup)
        self.root = Path(self.temp.name).resolve()
        self.source = self.root / "Sources/Resources/Legal"
        self.source.mkdir(parents=True)
        for name, original in LEGAL.items():
            data = ("synthetic fixture: " + name).encode()
            (self.root / original).write_bytes(data)
            (self.source / name).write_bytes(data)

    def test_stage_delivers_exact_texts(self):
        destination = self.root / "staged/Legal"
        stage(self.root, destination)
        check(self.root, destination)
        self.assertEqual(
            {p.name for p in destination.iterdir()}, set(LEGAL)
        )

    def test_stale_swiftpm_copy_blocks_packaging(self):
        (self.source / "GPL-3.0.txt").write_text("stale")
        with self.assertRaises(ValueError):
            stage(self.root, self.root / "staged/Legal")

    def test_missing_extra_and_symlink_are_rejected(self):
        victim = self.source / "GPL-3.0.txt"
        data = victim.read_bytes()
        victim.unlink()
        with self.assertRaises(ValueError):
            check(self.root, self.source)
        victim.write_bytes(data)
        extra = self.source / "unexpected"
        extra.write_text("extra")
        with self.assertRaises(ValueError):
            check(self.root, self.source)
        extra.unlink()
        victim.unlink()
        victim.symlink_to(self.root / "LICENSE")
        with self.assertRaises(ValueError):
            check(self.root, self.source)

if __name__ == "__main__":
    unittest.main()
```

##### Sources/Utils/LegalResources.swift

```swift
import Foundation

enum LegalDocument: String, CaseIterable {
    case license = "GPL-3.0"
    case notice = "Songbird-NOTICE"
    case thirdParty = "THIRD-PARTY-NOTICES"
    case source = "CORRESPONDING-SOURCE"
}

enum LegalResources {
    enum Failure: Error {
        case unavailable
    }

    static func read(_ document: LegalDocument) throws -> String {
        try resolve(
            packagedApplication:
                Bundle.main.bundleURL.pathExtension.lowercased() == "app",
            packaged: {
                try text(document, in: .main, directory: "Legal")
            },
            development: {
                // Intentionally lazy: this accessor can trap if absent.
                try text(
                    document, in: .module,
                    directory: "Resources/Legal"
                )
            }
        )
    }

    static func resolve(
        packagedApplication: Bool,
        packaged: () throws -> String?,
        development: () throws -> String?
    ) throws -> String {
        let value: String?
        if let packagedText = try packaged() {
            value = packagedText
        } else if packagedApplication {
            throw Failure.unavailable
        } else {
            value = try development()
        }
        guard let value,
              !value.trimmingCharacters(in: .whitespacesAndNewlines).isEmpty
        else {
            throw Failure.unavailable
        }
        return value
    }

    private static func text(
        _ document: LegalDocument,
        in bundle: Bundle,
        directory: String
    ) throws -> String? {
        guard let url = bundle.url(
            forResource: document.rawValue,
            withExtension: "txt",
            subdirectory: directory
        ) else {
            return nil
        }
        return try String(contentsOf: url, encoding: .utf8)
    }
}
```

##### Tests/SongbirdTests/LegalResourcesTests.swift

```swift
import XCTest
@testable import SongbirdLib

final class LegalResourcesTests: XCTestCase {
    func testPackagedTextWinsWithoutDevelopmentLookup() throws {
        let value = try LegalResources.resolve(
            packagedApplication: true,
            packaged: { "packaged" },
            development: {
                XCTFail("Development resource accessor must remain lazy")
                return "development"
            }
        )
        XCTAssertEqual(value, "packaged")
    }

    func testIncompleteAppNeverUsesDevelopmentFallback() {
        XCTAssertThrowsError(try LegalResources.resolve(
            packagedApplication: true,
            packaged: { nil },
            development: {
                XCTFail("Incomplete app must not consult .build")
                return "development"
            }
        ))
    }

    func testDevelopmentFallbackAndEmptyFailure() throws {
        XCTAssertEqual(try LegalResources.resolve(
            packagedApplication: false,
            packaged: { nil },
            development: { "development" }
        ), "development")

        XCTAssertThrowsError(try LegalResources.resolve(
            packagedApplication: false,
            packaged: { nil },
            development: { " \n" }
        ))
    }

    func testRealSwiftPMResourcesArePresent() throws {
        for document in LegalDocument.allCases {
            XCTAssertFalse(try LegalResources.read(document).isEmpty)
        }
        XCTAssertTrue(
            try LegalResources.read(.license)
                .contains("GNU GENERAL PUBLIC LICENSE")
        )
        XCTAssertTrue(
            try LegalResources.read(.notice)
                .contains("GPL-3.0-or-later")
        )
    }
}
```

##### Package.swift offline dependency selection

```swift
let grdbDependency: Package.Dependency =
    ProcessInfo.processInfo.environment["SONGBIRD_OFFLINE_DEPS"] == "1"
    ? .package(path: packageRoot + "/Vendor/GRDB.swift")
    : .package(
        url: "https://github.com/groue/GRDB.swift.git",
        from: "6.24.0"
    )
```

In Package.swift replace the existing single remote `.package(...)` entry with `grdbDependency`, preserving the product name and package identity at the SongbirdLib dependency. Unset `SPI_BUILDER` during offline verification: the actual pinned GRDB manifest conditionally adds swift-docc-plugin when that variable is 1. Normal builds retain the existing remote pin.

##### AboutSongbirdView.swift exact additions

Add the following state inside the existing public view:

```swift
@State private var showingLegal = false
@State private var legalNotice = "Legal notices unavailable."
```

Before the existing OK button:

```swift
Text(legalNotice)
    .font(.caption)
    .textSelection(.enabled)

Button("License, Notices & Source…") {
    showingLegal = true
}
.accessibilityIdentifier("legal.showDocuments")
```

Attach these modifiers to the existing outer content view:

```swift
.task {
    legalNotice = (try? LegalResources.read(.notice))
        ?? "Legal notices unavailable. This package is incomplete."
}
.sheet(isPresented: $showingLegal) {
    SongbirdLegalView()
}
```

Append this private sheet type in the same file:

```swift
private struct SongbirdLegalView: View {
    @Environment(\.dismiss) private var dismiss
    @State private var contents = "Loading legal notices…"

    var body: some View {
        VStack(alignment: .leading, spacing: 12) {
            Text("License, Notices & Source")
                .font(.headline)
            ScrollView {
                Text(contents)
                    .font(.system(.body, design: .monospaced))
                    .textSelection(.enabled)
                    .accessibilityIdentifier("legal.contents")
                    .frame(maxWidth: .infinity, alignment: .leading)
            }
            Button("Close") { dismiss() }
                .keyboardShortcut(.cancelAction)
                .accessibilityIdentifier("legal.close")
        }
        .padding()
        .frame(width: 640, height: 480)
        .task {
            do {
                contents = try LegalDocument.allCases.map {
                    $0.rawValue + "\n\n" + (try LegalResources.read($0))
                }.joined(separator: "\n\n────────\n\n")
            } catch {
                contents =
                    "Legal resources are missing or unreadable. " +
                    "This package is incomplete."
            }
        }
    }
}
```

Do not redesign About, replace the app menu, or force users through a click-through EULA. The About notice and sheet are readable without network access. Missing packaged texts yield an explicit incomplete-package message, never a development-bundle fallback.

#### Appendix P: source and archive integrity primitives

These checks prove selected bytes/membership, not legal permission or completeness of a mistakenly selected build graph. For outer publication rows carrying provenance metadata, project each row to exactly `path`, `sha256`, `bytes`, and `mode` before passing it to `verify_rows` or `verify_archive`.

##### scripts/publication_checks.py

```python
from pathlib import Path, PurePosixPath
import hashlib
import json
import stat
import tarfile

def relative(name):
    if not isinstance(name, str) or not name or "\\" in name:
        raise ValueError("Invalid path")
    path = PurePosixPath(name)
    if (
        path.is_absolute()
        or ".." in path.parts
        or path.as_posix() != name
        or name == "."
    ):
        raise ValueError(f"Noncanonical path: {name}")
    return path

def file_record(root, name):
    path = Path(root).joinpath(*relative(name).parts)
    if any(p.is_symlink() for p in (path, *path.parents)):
        raise ValueError(f"Symlink: {name}")
    info = path.stat()
    if not stat.S_ISREG(info.st_mode):
        raise ValueError(f"Not regular: {name}")
    data = path.read_bytes()
    return {
        "path": name,
        "sha256": hashlib.sha256(data).hexdigest(),
        "bytes": len(data),
        "mode": stat.S_IMODE(info.st_mode),
    }

def indexed(rows):
    result = {}
    for row in rows:
        name = row["path"]
        relative(name)
        if name in result:
            raise ValueError(f"Duplicate: {name}")
        result[name] = row
    return result

def verify_rows(root, rows):
    for name, row in indexed(rows).items():
        if file_record(root, name) != row:
            raise ValueError(f"Record mismatch: {name}")

def snapshot_digest(rows):
    indexed(rows)
    data = json.dumps(
        sorted(rows, key=lambda row: row["path"]),
        sort_keys=True, separators=(",", ":")
    ).encode()
    return hashlib.sha256(data).hexdigest()

def verify_component(root, component):
    if component["status"] != "verified":
        raise ValueError(f"Unverified component: {component['id']}")
    source_name = component["source_root"]
    source = Path(root).joinpath(*relative(source_name).parts)
    if source.is_symlink() or not source.is_dir():
        raise ValueError("Missing source tree")

    actual = set()
    for path in source.rglob("*"):
        if path.is_symlink():
            raise ValueError(f"Source symlink: {path}")
        if not path.is_dir():
            actual.add(path.relative_to(root).as_posix())

    rows = component["source_files"]
    if not rows or actual != set(indexed(rows)):
        raise ValueError("Source inventory mismatch")
    if snapshot_digest(rows) != component["snapshot_sha256"]:
        raise ValueError("Source snapshot identity mismatch")

    verify_rows(root, rows)
    for field in ("build_files", "binary_files", "evidence_files"):
        records = component[field]
        static_grdb = (field == "binary_files" and component["id"] == "grdb.swift"
                       and component.get("linkage") == "static")
        if not records and not static_grdb:
            raise ValueError(f"Missing {field}")
        verify_rows(root, records)

def verify_archive(fileobj, rows):
    wanted = indexed(rows)
    seen = set()
    with tarfile.open(fileobj=fileobj, mode="r:*") as archive:
        for member in archive:
            name = member.name
            relative(name)
            # Export contract: file members only; no links or directories.
            if not member.isfile() or name in seen or name not in wanted:
                raise ValueError(f"Unexpected archive member: {name}")
            seen.add(name)
            stream = archive.extractfile(member)
            if stream is None:
                raise ValueError(f"Unreadable member: {name}")
            data = stream.read()
            actual = {
                "path": name,
                "sha256": hashlib.sha256(data).hexdigest(),
                "bytes": len(data),
                "mode": member.mode,
            }
            if actual != wanted[name]:
                raise ValueError(f"Archive mismatch: {name}")
    if seen != set(wanted):
        raise ValueError("Incomplete archive")
```

##### scripts/tests/test_publication_checks.py

```python
import io
import sys
import tarfile
import tempfile
import unittest
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parents[1]))
from publication_checks import file_record, verify_rows, verify_archive

def fixture_tar(entries):
    output = io.BytesIO()
    with tarfile.open(fileobj=output, mode="w") as archive:
        for name, data, kind in entries:
            info = tarfile.TarInfo(name)
            info.mode = 0o644
            info.type = kind
            if kind == tarfile.REGTYPE:
                info.size = len(data)
                archive.addfile(info, io.BytesIO(data))
            else:
                info.linkname = "source.txt"
                archive.addfile(info)
    output.seek(0)
    return output

class PublicationChecksTests(unittest.TestCase):
    def test_rows_and_archive_reject_mutations(self):
        with tempfile.TemporaryDirectory() as directory:
            root = Path(directory).resolve()
            path = root / "source.txt"
            path.write_bytes(b"synthetic source fixture")
            path.chmod(0o644)
            row = file_record(root, "source.txt")
            verify_rows(root, [row])

            good = ("source.txt", path.read_bytes(), tarfile.REGTYPE)
            verify_archive(fixture_tar([good]), [row])

            bad_cases = [
                [],
                [good, good],
                [good, ("extra", b"x", tarfile.REGTYPE)],
                [("source.txt", b"changed", tarfile.REGTYPE)],
                [("../source.txt", b"x", tarfile.REGTYPE)],
                [("source.txt", b"", tarfile.SYMTYPE)],
                [("source.txt", b"", tarfile.LNKTYPE)],
            ]
            for entries in bad_cases:
                with self.subTest(entries=entries):
                    with self.assertRaises(ValueError):
                        verify_archive(fixture_tar(entries), [row])

            path.write_bytes(b"changed")
            with self.assertRaises(ValueError):
                verify_rows(root, [row])

if __name__ == "__main__":
    unittest.main()
```

#### Appendix D: complete small Discogs primitives and initial regression tests

The call-site changes in D5–D24 are mandatory consumers of these helpers. A helper-only green suite does not close the feature. All code remains proposed/uncompiled in this planning turn.

##### Sources/Discogs/DiscogsFreshness.swift

```swift
import Foundation
import Darwin

public struct DiscogsFetchStamp: Codable, Hashable, Sendable {
    public let wall: Date
    public let continuousSeconds: TimeInterval
    public let bootID: String

    public init(wall: Date, continuousSeconds: TimeInterval, bootID: String) {
        self.wall = wall
        self.continuousSeconds = continuousSeconds
        self.bootID = bootID
    }
}

public enum DiscogsFreshness {
    public static let maximumAge: TimeInterval = 5 * 60 * 60

    public static func isFresh(
        _ stamp: DiscogsFetchStamp?,
        at now: DiscogsFetchStamp?,
        maximumAge: TimeInterval = DiscogsFreshness.maximumAge
    ) -> Bool {
        guard let stamp, let now,
              !stamp.bootID.isEmpty,
              stamp.bootID == now.bootID,
              maximumAge.isFinite, maximumAge > 0 else {
            return false
        }

        let limit = min(maximumAge, Self.maximumAge)
        let wallAge = now.wall.timeIntervalSince(stamp.wall)
        let continuousAge = now.continuousSeconds - stamp.continuousSeconds

        return wallAge.isFinite && continuousAge.isFinite
            && wallAge >= 0 && wallAge < limit
            && continuousAge >= 0 && continuousAge < limit
    }
}

public enum DiscogsClock {
    public static func sample() -> DiscogsFetchStamp? {
        var size = 0
        guard sysctlbyname("kern.bootsessionuuid", nil, &size, nil, 0) == 0,
              size > 1 else { return nil }

        var bytes = [CChar](repeating: 0, count: size)
        let status = bytes.withUnsafeMutableBytes {
            sysctlbyname("kern.bootsessionuuid", $0.baseAddress, &size, nil, 0)
        }
        guard status == 0 else { return nil }

        var timebase = mach_timebase_info_data_t()
        guard mach_timebase_info(&timebase) == KERN_SUCCESS,
              timebase.denom != 0 else { return nil }

        let seconds = Double(mach_continuous_time())
            * Double(timebase.numer) / Double(timebase.denom)
            / 1_000_000_000
        let bootID = bytes.withUnsafeBufferPointer {
            String(cString: $0.baseAddress!)
        }
        guard !bootID.isEmpty, seconds.isFinite else { return nil }

        return DiscogsFetchStamp(
            wall: Date(),
            continuousSeconds: seconds,
            bootID: bootID
        )
    }
}

public struct DiscogsContentEvidence: Codable, Hashable, Sendable {
    public let releaseID: Int
    public let fetches: [DiscogsFetchStamp]

    public init(releaseID: Int, fetches: [DiscogsFetchStamp]) {
        self.releaseID = releaseID
        self.fetches = fetches
    }

    public var sourcePageURL: URL? {
        guard releaseID > 0 else { return nil }
        return URL(string: "https://www.discogs.com/release/\(releaseID)")
    }

    public func isFresh(at now: DiscogsFetchStamp?) -> Bool {
        releaseID > 0 && !fetches.isEmpty
            && fetches.allSatisfy { DiscogsFreshness.isFresh($0, at: now) }
    }
}

func requireFreshDiscogsEvidence(
    _ evidence: [DiscogsContentEvidence],
    clock: @Sendable () -> DiscogsFetchStamp? = { DiscogsClock.sample() }
) throws {
    guard !evidence.isEmpty else { return }
    let now = clock()
    guard evidence.allSatisfy({ $0.isFresh(at: now) }) else {
        throw LibraryHealthMutationError.remoteEvidenceExpired
    }
}
```

`kern.bootsessionuuid` and `mach_continuous_time()` were inspected as planning prerequisites; their Swift integration still needs compilation/tests. Cache reuse across processes in the same boot is permitted only within both elapsed-clock limits. A reboot invalidates old responses. Do not use this clock from render/decode callbacks or sample it inside every SwiftUI row.

##### Tests/SongbirdTests/DiscogsFreshnessTests.swift

```swift
import XCTest
@testable import SongbirdLib

final class DiscogsFreshnessTests: XCTestCase {
    private func stamp(
        wall: TimeInterval,
        continuous: TimeInterval,
        boot: String = "boot-A"
    ) -> DiscogsFetchStamp {
        DiscogsFetchStamp(
            wall: Date(timeIntervalSince1970: wall),
            continuousSeconds: continuous,
            bootID: boot
        )
    }

    func testFreshBeforeBoundaryAndExpiredAtBoundary() {
        let start = stamp(wall: 1_000, continuous: 100)

        XCTAssertTrue(DiscogsFreshness.isFresh(
            start,
            at: stamp(wall: 1_059, continuous: 159),
            maximumAge: 60
        ))
        XCTAssertFalse(DiscogsFreshness.isFresh(
            start,
            at: stamp(wall: 1_060, continuous: 160),
            maximumAge: 60
        ))
    }

    func testWallClockRollbackCannotExtendLifetime() {
        let start = stamp(wall: 1_000, continuous: 100)

        XCTAssertFalse(DiscogsFreshness.isFresh(
            start,
            at: stamp(wall: 1_010, continuous: 161),
            maximumAge: 60
        ))
        XCTAssertFalse(DiscogsFreshness.isFresh(
            start,
            at: stamp(wall: 999, continuous: 101),
            maximumAge: 60
        ))
    }

    func testDifferentBootAndMissingClockFailClosed() {
        let start = stamp(wall: 1_000, continuous: 100)

        XCTAssertFalse(DiscogsFreshness.isFresh(
            start,
            at: stamp(wall: 1_001, continuous: 101, boot: "boot-B")
        ))
        XCTAssertFalse(DiscogsFreshness.isFresh(start, at: nil))
        XCTAssertFalse(DiscogsFreshness.isFresh(
            nil,
            at: stamp(wall: 1_001, continuous: 101)
        ))
    }
}
```

Add separate assertions for empty `fetches`, one fresh plus one expired stamp, invalid/nonpositive release ID, non-finite monotonic values, and requested maximumAge exceeding the five-hour cap. Keep these tests per-instance and deterministic.

##### Initial existing-cache regressions (D1/D2)

Add inside DiscogsArtworkSearchCacheTests, before changing that API:

```swift
func testFutureDatedCacheEntryIsRejected() async {
    let fileURL = FileManager.default.temporaryDirectory
        .appendingPathComponent("discogs-future-\(UUID().uuidString).json")
    defer { try? FileManager.default.removeItem(at: fileURL) }

    let cache = DiscogsArtworkSearchCache(fileURL: fileURL, maximumAge: 60)
    let query = DiscogsArtworkSearchQuery(
        albumTitle: "Album",
        albumArtist: "Artist"
    )
    let savedAt = Date(timeIntervalSince1970: 1_000)
    await cache.store([], for: query, now: savedAt)

    let result = await cache.candidates(
        for: query,
        now: savedAt.addingTimeInterval(-1)
    )

    XCTAssertNil(result)
}

func testCacheExpiresAtBoundary() async {
    let fileURL = FileManager.default.temporaryDirectory
        .appendingPathComponent("discogs-boundary-\(UUID().uuidString).json")
    defer { try? FileManager.default.removeItem(at: fileURL) }

    let cache = DiscogsArtworkSearchCache(fileURL: fileURL, maximumAge: 60)
    let query = DiscogsArtworkSearchQuery(
        albumTitle: "Album",
        albumArtist: "Artist"
    )
    let savedAt = Date(timeIntervalSince1970: 1_000)
    await cache.store([], for: query, now: savedAt)

    let result = await cache.candidates(
        for: query,
        now: savedAt.addingTimeInterval(60)
    )

    XCTAssertNil(result)
}
```

##### Sources/Discogs/DiscogsAttributionView.swift

```swift
import SwiftUI

struct DiscogsAttributionView: View {
    let sourcePageURL: URL

    var body: some View {
        Link("Data provided by Discogs.", destination: sourcePageURL)
            .font(.caption)
            .accessibilityLabel("Data provided by Discogs.")
            .accessibilityHint("Opens the source release on Discogs")
            .accessibilityIdentifier("discogs.sourceLink")
    }
}
```

Do not pass an arbitrary API resource_url. The URL comes from the positive release ID; the data and link disappear together when the evidence is stale. When several links share the identifier, use the containing result row to disambiguate in UI tests; do not select an arbitrary link.

##### LibraryArtworkChange exact initializer delta

In `Sources/Library/LibraryHealthMutationService.swift`, add the stored property and replace the existing explicit initializer with:

```swift
    public let discogsEvidence: DiscogsContentEvidence?

    public init(
        albumID: UUID,
        expectedArtworkDigest: String?,
        imageData: Data,
        discogsEvidence: DiscogsContentEvidence? = nil
    ) {
        self.albumID = albumID
        self.expectedArtworkDigest = expectedArtworkDigest
        self.imageData = imageData
        self.discogsEvidence = discogsEvidence
    }
```

Add `case remoteEvidenceExpired` to LibraryHealthMutationError and its description `"Discogs results expired. Search again."`. In DiscogsError, add `case resultsExpired` with the same description and `case freshnessUnavailable` with `"Discogs freshness could not be verified. Try again."`. Keep those typed errors distinguishable from network failures; do not retry an expired request as if it were a transient image transport failure.

##### Sources/Discogs/DiscogsHTTP.swift

```swift
import Foundation

public enum DiscogsHTTP {
    public static func makeSession() -> URLSession {
        let configuration = URLSessionConfiguration.ephemeral
        configuration.urlCache = nil
        configuration.requestCachePolicy = .reloadIgnoringLocalCacheData
        return URLSession(configuration: configuration)
    }

    static func request(url: URL, userAgent: String) -> URLRequest {
        var request = URLRequest(url: url, cachePolicy: .reloadIgnoringLocalCacheData,
                                 timeoutInterval: 30)
        request.setValue(userAgent, forHTTPHeaderField: "User-Agent")
        request.setValue("no-cache", forHTTPHeaderField: "Cache-Control")
        return request
    }
}
```

Make DiscogsClient's default session use this factory, while preserving explicit injected sessions for URLProtocol tests. Use the request helper in all three existing request constructors. An injected test session must also receive explicit request cache policy/no-cache headers. Do not modify URLSession.shared globally or disable caching for unrelated application traffic.

##### Tests/SongbirdTests/DiscogsClientTests.swift initial production-URL regression

```swift
import Foundation
import XCTest
@testable import SongbirdLib

private final class DiscogsFixtureProtocol: URLProtocol {
    override class func canInit(with request: URLRequest) -> Bool {
        request.url?.host == "api.discogs.com"
    }
    override class func canonicalRequest(for request: URLRequest) -> URLRequest { request }
    override func startLoading() {
        let response = HTTPURLResponse(url: request.url!, statusCode: 200,
                                       httpVersion: "HTTP/1.1",
                                       headerFields: ["Content-Type": "application/json"])!
        let payload = Data("""
        {"results":[{"id":42,"title":"Artist - Album",
        "cover_image":"https://example.invalid/cover.png",
        "resource_url":"https://api.discogs.com/releases/42"}]}
        """.utf8)
        client?.urlProtocol(self, didReceive: response, cacheStoragePolicy: .notAllowed)
        client?.urlProtocol(self, didLoad: payload)
        client?.urlProtocolDidFinishLoading(self)
    }
    override func stopLoading() {}
}

final class DiscogsClientTests: XCTestCase {
    func testActualSearchBuildsHumanReleaseURLFromID() async throws {
        let configuration = URLSessionConfiguration.ephemeral
        configuration.protocolClasses = [DiscogsFixtureProtocol.self]
        let session = URLSession(configuration: configuration)
        defer { session.invalidateAndCancel() }
        let client = DiscogsClient(session: session, tokenProvider: { "synthetic-test-token" })
        let page = try await client.search(
            DiscogsArtworkSearchQuery(albumTitle: "Album", albumArtist: "Artist"), page: 1
        )
        let candidate = try XCTUnwrap(page.candidates.first)
        XCTAssertEqual(candidate.sourcePageURL.absoluteString,
                       "https://www.discogs.com/release/42")
    }
}
```

This fixture intercepts all actual requests to the known API host and has no real credentials. Extend it without global mutable handlers: use distinct deterministic fixture query values or a per-instance synchronized fixture object. Add tests for relative/missing/unrelated resource_url and invalid IDs, plus cache headers and delayed-response expiry. Do not use the real service to get RED.

##### Sources/Discogs/DiscogsResponseCache.swift — reusable cache kernel

Both the search cache and the explicitly Discogs-only CD cache path use this kernel. Their small wrappers retain existing key normalization and provider directory ownership. This avoids implementing two different expiry algorithms.

```swift
import Foundation

actor DiscogsResponseCache<Value: Codable & Sendable> {
    struct Hit: Codable, Sendable {
        let value: Value
        let fetches: [DiscogsFetchStamp]
    }
    private struct Envelope: Codable {
        let version: Int
        let entries: [String: Hit]
    }
    private let fileURL: URL
    private let maximumAge: TimeInterval
    private let clock: @Sendable () -> DiscogsFetchStamp?
    private var entries: [String: Hit]?

    init(fileURL: URL,
         maximumAge: TimeInterval = DiscogsFreshness.maximumAge,
         clock: @escaping @Sendable () -> DiscogsFetchStamp? = { DiscogsClock.sample() }) {
        self.fileURL = fileURL
        self.maximumAge = maximumAge
        self.clock = clock
    }

    func value(for key: String) -> Hit? {
        loadIfNeeded()
        let now = clock()
        prune(at: now)
        return entries?[key]
    }

    @discardableResult
    func store(_ value: Value, fetches: [DiscogsFetchStamp], for key: String) -> Bool {
        loadIfNeeded()
        let now = clock()
        prune(at: now)
        let hit = Hit(value: value, fetches: fetches)
        guard fresh(hit, at: now) else { return false }
        entries?[key] = hit
        persist()
        return true
    }

    private func fresh(_ hit: Hit, at now: DiscogsFetchStamp?) -> Bool {
        !hit.fetches.isEmpty && hit.fetches.allSatisfy {
            DiscogsFreshness.isFresh($0, at: now, maximumAge: maximumAge)
        }
    }

    private func loadIfNeeded() {
        guard entries == nil else { return }
        guard let data = try? Data(contentsOf: fileURL),
              let envelope = try? JSONDecoder().decode(Envelope.self, from: data),
              envelope.version == 2 else {
            entries = [:]
            // This file is an explicitly owned Discogs response cache, not a library.
            persist()
            return
        }
        entries = envelope.entries
    }

    private func prune(at now: DiscogsFetchStamp?) {
        guard let old = entries else { return }
        let valid = old.filter { fresh($0.value, at: now) }
        if valid.count != old.count {
            entries = valid
            persist()
        }
    }

    private func persist() {
        guard let entries,
              let data = try? JSONEncoder().encode(Envelope(version: 2, entries: entries)) else {
            return
        }
        do {
            try FileManager.default.createDirectory(
                at: fileURL.deletingLastPathComponent(), withIntermediateDirectories: true
            )
            try data.write(to: fileURL, options: .atomic)
        } catch {
            // Disk-cache failure must not turn stale content into a usable result.
            // Keep this cache best-effort; do not log URLs/tokens or touch the library.
        }
    }
}
```

For `DiscogsArtworkSearchCache`, replace the old dictionary implementation with an owned kernel instance for `[DiscogsArtworkCandidate]`. Expose a stamped cache-hit method and a store method requiring the caller's original page stamp; remove the old `now: Date` store overload after migrating all consumers so reminting cannot remain a convenience path. For the CD path, use a separately owned versioned cache file under the existing Discogs-specific directory. Add explicit Discogs methods to the generic cache adapter or inject a dedicated wrapper; in either case the provider must validate stamps even when given a long-lifetime cache. Never wipe the generic MusicBrainz cache directory as migration cleanup.

##### Sources/Discogs/DiscogsFreshnessView.swift — one clock owner per surface

```swift
import AppKit
import SwiftUI

@MainActor
struct DiscogsFreshnessView<Content: View>: View {
    private let clock: () -> DiscogsFetchStamp?
    private let onTick: (DiscogsFetchStamp?) -> Void
    private let content: (DiscogsFetchStamp?) -> Content
    @State private var now: DiscogsFetchStamp?

    init(clock: @escaping () -> DiscogsFetchStamp? = { DiscogsClock.sample() },
         onTick: @escaping (DiscogsFetchStamp?) -> Void,
         @ViewBuilder content: @escaping (DiscogsFetchStamp?) -> Content) {
        self.clock = clock
        self.onTick = onTick
        self.content = content
        _now = State(initialValue: clock())
    }

    var body: some View {
        content(now)
            .task {
                refresh()
                while !Task.isCancelled {
                    do { try await Task.sleep(for: .seconds(30)) }
                    catch { return }
                    refresh()
                }
            }
            .onReceive(NotificationCenter.default.publisher(
                for: NSApplication.didBecomeActiveNotification
            )) { _ in refresh() }
            .onReceive(NSWorkspace.shared.notificationCenter.publisher(
                for: NSWorkspace.didWakeNotification
            )) { _ in refresh() }
    }

    private func refresh() {
        let sample = clock()
        now = sample
        onTick(sample)
    }
}
```

Wrap the existing result container once, passing its exact `expireResults(at:)` method as onTick and filtering display values using the provided sample. The separate CD service and thumbnail actor still require ownership-level expiry as specified in D16/D18; do not claim this view helper covers hidden service state. Final apply/download checks always sample the clock independently.

#### Appendix L2: real legal-input preflight

Add `scripts/tests/test_legal_inputs.py` before L1's canonical texts. During the initial local bootstrap set `GRDB_SOURCE=${CANDIDATE_PARENT}/songbird-public-verification/scratch/checkouts/GRDB.swift`; after S6, use the verified `Vendor/GRDB.swift` source snapshot instead. This path is a read-only license input, not a substitute for source-delivery provenance.

```python
import json
import os
from pathlib import Path
import unittest

ROOT = Path(__file__).resolve().parents[2]

class LegalInputTests(unittest.TestCase):
    def test_full_license_and_actual_component_notices(self):
        license_path = ROOT / 'LICENSE'
        self.assertTrue(license_path.is_file(), 'Full project GPL text is missing')
        if not license_path.is_file():
            return
        self.assertEqual(license_path.read_bytes(),
                         (ROOT / 'Vendor/aubio/src/COPYING').read_bytes())
        notice = (ROOT / 'NOTICE.txt').read_text(encoding='utf-8')
        self.assertIn('GPL-3.0-or-later', notice)
        self.assertIn('WITHOUT ANY WARRANTY', notice)
        self.assertIn('redistribute', notice)
        third_party = (ROOT / 'THIRD_PARTY_NOTICES.txt').read_text(encoding='utf-8')
        self.assertIn((ROOT / 'Vendor/FLAC/NOTICE.txt').read_text(encoding='utf-8'), third_party)
        grdb = Path(os.environ.get('GRDB_SOURCE', str(ROOT / 'Vendor/GRDB.swift'))).resolve()
        self.assertTrue((grdb / 'LICENSE').is_file(), 'Supply the verified GRDB source snapshot')
        self.assertIn((grdb / 'LICENSE').read_text(encoding='utf-8'), third_party)
        pins = json.loads((ROOT / 'Package.resolved').read_text())['pins']
        pin = next(p for p in pins if p['identity'] == 'grdb.swift')
        self.assertEqual(pin['state']['revision'],
                         '2cf6c756e1e5ef6901ebae16576a7e4e4b834622')

if __name__ == '__main__':
    unittest.main()
```

This check intentionally does not assert an invented copyright owner or certify Ooura rights. Add those source-review records only after actual evidence is obtained.

#### Appendix P2: concrete release gate file and checker

Create `publication/release-gates.json` with this initial real state, not example approvals:

```json
{
  "schema": 1,
  "gates": {
    "original-code-authority": {"status": "pending", "evidence_files": []},
    "source-correspondence": {"status": "pending", "evidence_files": []},
    "asset-provenance": {"status": "pending", "evidence_files": []},
    "brand-clearance": {"status": "pending", "evidence_files": []},
    "discogs-persistent-import-policy": {"status": "pending", "evidence_files": []},
    "discogs-system-display-policy": {"status": "pending", "evidence_files": []},
    "notice-delivery": {"status": "pending", "evidence_files": []},
    "privacy-contact-review": {"status": "pending", "evidence_files": []}
  }
}
```

`evidence_files` uses the same exact four-field file-record format. Technical checks may prepare evidence, but must never flip a human rights/policy gate to verified automatically. Actual documented owner/legal review is required; no invented approval text. A verifier can check the existence and identity of the evidence, not decide trademark law or grant permission.

Add `scripts/check-release-gates.py`:

```python
#!/usr/bin/env python3
import json
from pathlib import Path
import sys
from publication_checks import verify_rows

REQUIRED = {
    'original-code-authority', 'source-correspondence', 'asset-provenance',
    'brand-clearance', 'discogs-persistent-import-policy',
    'discogs-system-display-policy', 'notice-delivery', 'privacy-contact-review',
}

def check(root):
    root = Path(root).resolve(strict=True)
    path = root / 'publication/release-gates.json'
    if path.is_symlink():
        raise ValueError('Release gate file must not be a symlink')
    value = json.loads(path.read_text(encoding='utf-8'))
    if value.get('schema') != 1 or set(value.get('gates', {})) != REQUIRED:
        raise ValueError('Release gate set/schema mismatch')
    blocked = []
    for name in sorted(REQUIRED):
        gate = value['gates'][name]
        if gate.get('status') != 'verified' or not gate.get('evidence_files'):
            blocked.append(name)
            continue
        verify_rows(root, gate['evidence_files'])
    if blocked:
        raise ValueError('Release blocked: ' + ', '.join(blocked))

if __name__ == '__main__':
    if len(sys.argv) != 2:
        raise SystemExit('Usage: check-release-gates.py ROOT')
    try:
        check(sys.argv[1])
    except (ValueError, OSError, KeyError, TypeError) as error:
        raise SystemExit(str(error))
    print('Release gate evidence verified; this check does not grant legal rights')
```

Add fixture tests for missing gate, unknown gate, pending status, empty evidence list, altered evidence bytes, and a complete **synthetic** evidence fixture. Synthetic fixtures may exercise verified status; production evidence may not be synthesized. Command: `python3 -m unittest discover -s scripts/tests -p test_release_gates.py -v`.

#### Appendix P3: complete candidate phase verifier

Add `scripts/verify-publication.py` during P2. This is the integrity checker, not an auto-approver or an auto-sealer. `publication/changes.json` must be reviewed when generating the new manifest. Do not automatically accept everything currently found on disk.

```python
#!/usr/bin/env python3
import argparse
import hashlib
import json
import os
from pathlib import Path
import subprocess
from publication_checks import file_record, relative, verify_rows, verify_archive

FIELDS = ('path', 'sha256', 'bytes', 'mode')
BANNED_ROOTS = {
    '.hermes', '.cursor', '.mimocode', '.build', '.swiftpm',
    'Resources', 'songbird-images', 'carousel', 'Cog-main',
    'Swinsian.app', 'Nightingale.app', 'Nightingale',
}

def git(root, *args, allowed=(0,)):
    result = subprocess.run(['git', '--no-optional-locks', '-C', str(root), *args],
                            capture_output=True)
    if result.returncode not in allowed:
        raise ValueError('Git command failed: ' + ' '.join(args))
    return result

def safe_payload_name(name):
    path = relative(name)
    if (path.parts[0] in BANNED_ROOTS
            or path.parts[0].startswith(('nightingale-media-player-', 'museeks-'))
            or any(p in {'.git', '__pycache__', '.DS_Store'} for p in path.parts)
            or any(p.endswith('.app') for p in path.parts)
            or any(p == '.env' or p.startswith('.env.') for p in path.parts)
            or name.startswith('Usability/reports/')):
        raise ValueError('Excluded payload path: ' + name)

def manifest_rows(root):
    value = json.loads((root / 'publication/manifest.json').read_text(encoding='utf-8'))
    rows = [{key: row[key] for key in FIELDS} for row in value['files']]
    names = [row['path'] for row in rows]
    if len(names) != len(set(names)) or 'publication/manifest.json' in names:
        raise ValueError('Duplicate/self-referential manifest entries')
    for name in names:
        safe_payload_name(name)
    rows.append(file_record(root, 'publication/manifest.json'))
    return rows

def eligible(root):
    data = git(root, 'ls-files', '--cached', '--others', '--exclude-standard', '-z').stdout
    return set(filter(None, data.decode('utf-8').split('\0')))

def reject_retired_assets(root, rows):
    retired = json.loads((root / 'publication/retired-asset-hashes.json').read_text())
    forbidden = set(retired['sha256'])
    for row in rows:
        if row['sha256'] in forbidden:
            raise ValueError('Retired artwork still present: ' + row['path'])

def no_external_git_linkage(root):
    for variable in ('GIT_DIR', 'GIT_WORK_TREE', 'GIT_OBJECT_DIRECTORY',
                     'GIT_ALTERNATE_OBJECT_DIRECTORIES'):
        if os.environ.get(variable):
            raise ValueError('Unset external Git routing variable: ' + variable)
    directory = root / '.git'
    if directory.is_symlink() or not directory.is_dir():
        raise ValueError('Candidate must own its Git directory')
    for name in ('commondir', 'gitdir', 'objects/info/alternates'):
        if (directory / name).exists():
            raise ValueError('Inherited Git linkage: ' + name)

def pre_seed(root):
    if git(root, 'symbolic-ref', '--short', 'HEAD').stdout.strip() != b'main':
        raise ValueError('Expected unborn main')
    if git(root, 'rev-parse', '--verify', 'HEAD', allowed=(0, 128)).returncode == 0:
        raise ValueError('Initial commit already exists')
    for args in [('for-each-ref', '--format=%(refname)'), ('remote',), ('ls-files',)]:
        if git(root, *args).stdout.strip():
            raise ValueError('Pre-seed refs/remotes/index must be empty')
    counts = dict(line.split(': ', 1) for line in
                  git(root, 'count-objects', '-v').stdout.decode().splitlines() if ': ' in line)
    if any(counts.get(key) != '0' for key in ('count', 'in-pack', 'packs', 'garbage')):
        raise ValueError('Pre-seed repository contains objects')

def check_index(root, rows):
    wanted = {row['path']: row for row in rows}
    found = {}
    records = git(root, 'ls-files', '--stage', '-z').stdout.split(b'\0')
    for record in filter(None, records):
        metadata, raw_name = record.split(b'\t', 1)
        mode, object_id, stage = metadata.decode().split()
        name = raw_name.decode('utf-8')
        safe_payload_name(name)
        if stage != '0' or name in found or name not in wanted:
            raise ValueError('Unexpected/conflicted index entry: ' + name)
        data = git(root, 'cat-file', 'blob', object_id).stdout
        row = wanted[name]
        expected_mode = '100755' if row['mode'] & 0o111 else '100644'
        if (mode != expected_mode or len(data) != row['bytes']
                or hashlib.sha256(data).hexdigest() != row['sha256']):
            raise ValueError('Staged blob/mode mismatch: ' + name)
        found[name] = object_id
    if set(found) != set(wanted):
        raise ValueError('Staged payload is incomplete')

def check(root, phase, archive=None):
    root = Path(root).resolve(strict=True)
    no_external_git_linkage(root)
    rows = manifest_rows(root)
    wanted = {row['path'] for row in rows}
    if eligible(root) != wanted:
        raise ValueError('Git-eligible files differ from explicit payload')
    verify_rows(root, rows)
    reject_retired_assets(root, rows)
    if phase == 'pre-seed':
        pre_seed(root)
    elif phase == 'index':
        check_index(root, rows)
    elif phase == 'archive':
        if archive is None:
            raise ValueError('Archive path required')
        with Path(archive).open('rb') as handle:
            verify_archive(handle, rows)

if __name__ == '__main__':
    parser = argparse.ArgumentParser()
    parser.add_argument('phase', choices=('pre-seed', 'index', 'development', 'archive'))
    parser.add_argument('root')
    parser.add_argument('archive', nargs='?')
    args = parser.parse_args()
    try:
        check(args.root, args.phase, args.archive)
    except (ValueError, OSError, KeyError, TypeError) as error:
        raise SystemExit(str(error))
    print('Publication payload verified: ' + args.phase)
```

This checker deliberately retains the manifest's self-hash exception. For actual release distribution, record its hash in an external receipt and run both component/source checks and `check-release-gates.py`; `archive` integrity alone does not authorize release. For the index, Git tracks executable/non-executable mode, not arbitrary filesystem ownership/group permission metadata; the tar check still verifies the recorded archive mode.

Create the local source archive only from already verified rows, never `tar .` or a generic automatic GitHub archive that omits required dependency sources:

```sh
python3 - "$RUN/corresponding-source.tar" <<'PY'
import io, json, pathlib, sys, tarfile
sys.path.insert(0, 'scripts')
from publication_checks import file_record, verify_rows
root = pathlib.Path.cwd().resolve()
manifest = json.loads((root / 'publication/manifest.json').read_text())
keys = ('path', 'sha256', 'bytes', 'mode')
rows = [{key: row[key] for key in keys} for row in manifest['files']]
rows.append(file_record(root, 'publication/manifest.json'))
verify_rows(root, rows)
with tarfile.open(sys.argv[1], mode='x') as archive:
    for row in sorted(rows, key=lambda row: row['path']):
        data = (root / row['path']).read_bytes()
        info = tarfile.TarInfo(row['path'])
        info.size, info.mode = len(data), row['mode']
        info.uid = info.gid = info.mtime = 0
        info.uname = info.gname = ''
        archive.addfile(info, io.BytesIO(data))
PY
python3 scripts/verify-publication.py archive . "$RUN/corresponding-source.tar"
```

No external upload occurs. Retain an actual hash/identity receipt next to the local archive, outside it; later release directions must point to the actual reviewed artifact. A retry uses a new output name or an explicitly removed owned artifact; mode `x` does not overwrite previous evidence.

#### Additional D wiring details that must not be skipped

1. In `DiscogsArtworkCandidate` add this computed property after mandatory fetchedAt exists; preserve canonical `sourcePageURL` from the real client and normalize invalid old caches by rejection, not by reminting timestamps:

```swift
    public var evidence: DiscogsContentEvidence {
        DiscogsContentEvidence(releaseID: id, fetches: [fetchedAt])
    }
```

2. Successful HTTP-200 empty pages have the page's fetchedAt and can be negative-cached. Do not manufacture a fresh timestamp for token/network failures or a 404/noResults exception that currently carries no acquisition stamp. Such failures are not cached; a later deliberate request may retry. A no-CD-Text local inability to search is not a cacheable API answer.
3. Manual and bulk result thumbnails currently use `AsyncImage`, which does not use the new Discogs session policy. After D18, replace those exact thumbnail branches in `DiscogsSearchView.candidateRow` and `DiscogsBulkReviewView.candidateButton` with the provider-aware existing thumbnail view, retaining the no-image placeholder branch:

```swift
ArtworkThumbnailView(
    reference: .discogsRemote(thumbURL, evidence: candidate.evidence),
    pointSize: CGSize(width: 52, height: 52),
    accessibilityLabel: "Discogs release artwork",
    cornerRadius: 4,
    placeholderSymbol: "photo"
)
```

Keep the source Link outside the selection Button. ArtworkThumbnailView currently calls `.accessibilityElement(children: .ignore)` on its image: place any adjacent attribution outside that ignored subtree and outside the image's fixed frame, or it will be invisible to accessibility even if visually drawn. Do not embed duplicate links both inside this reusable image and in its result row; each attributed surface has one appropriate source link.
4. Clock-controlled image and metadata fixtures must exercise the actual producer→cache→view model→mutation flow. Do not “prove” the whole path only by asserting DiscogsFreshness.isFresh.
5. For loaded SwiftUI state, extract a small testable expiry state operation if necessary, but preserve existing ViewTaskSlot, generation, stable UUID and cancellation boundaries. Do not use an application-wide timer to schedule audio transitions.
6. The CD metadata composite has no extra outer cache, but DiscogsAudioCDMetadataProvider's metadataCache is a real bypass. Keep the distinction in tests and documentation.

#### Tests / validation and final handoff

##### V1 — deterministic combined-tree verification

From the candidate and the common isolated harness, after the technical tasks:

```sh
python3 -m unittest discover -s scripts/tests -p 'test_legal_*.py' -v
python3 -m unittest discover -s scripts/tests -p 'test_publication_checks.py' -v
python3 -m unittest discover -s scripts/tests -p 'test_verify_publication.py' -v
python3 -m unittest discover -s scripts/tests -p 'test_release_gates.py' -v
python3 scripts/legal_resources.py check . Sources/Resources/Legal
quick
swift build -c release --scratch-path "$RUN/scratch"
env -u SPI_BUILDER SONGBIRD_OFFLINE_DEPS=1 \
  swift build -c release --scratch-path "$RUN/offline-final"
./scripts/validate-agent-harness.sh README.md SOURCE_CODE.md docs/LICENSE_SCOPE.md \
  docs/DISCOGS_CONTENT_POLICY.md docs/exec-plans/active/release-licensing-remediation.md
```

Expected: real named tests execute, no failures or new unexplained skips; ordinary and offline builds succeed; local links and canonical/legal resource bytes match. The two pre-existing optional store-fixture skips may remain without importing user databases. If vendor/audio/CD/playback-adjacent implementation changed, also run `./scripts/test-audio-tsan.sh` under the isolated home and record the actual result. Hardware/device acceptance remains separate.

The release gate checker is expected to **fail** until actual source/rights/policy/privacy evidence exists. Do not weaken it to obtain an all-green development command. Record technical-complete and release-blocked states separately.

##### V2 — disposable UI acceptance, explicitly scoped

This is the only UI acceptance path; source/tests cannot certify rendered attribution or About behavior. The existing runner packages, locally signs, and launches, so obtain approval for that isolated operation separately if signing/launch remains excluded. Do not modify signing machinery as part of this plan or claim release-trust validation.

After approval:

```sh
./scripts/ai-usability audit --fixture standard --prepare-only --keep
```

Use only its returned run.json session/profile/PIDs and the existing `scripts/usability/ui` contract. Begin with `policy` and `window-snapshot`, open the existing About command, then activate `id=legal.showDocuments`; verify the full texts are readable/selectable, warranty/redistribution information is visible, and Close works. Test a separately staged incomplete packaged app through the loader seam without launching the normal application; never move/delete the real `.build` or installed app to simulate missing resources.

Discogs UI acceptance requires deterministic canned responses through the existing client injection seams—no live token or API call. The current usability fixture does **not** automatically provide that seam to every window. Add a narrowly scoped test-only fixture/client wiring task before the UI replay, or leave this gate pending; do not claim that a live search is authorized or bypass the wrapper's blocked external-search controls. New fixture wiring must be tied to the existing disposable testing identity, cannot fall back to real credentials, and needs its own contract test. Do not add a production network bypass switch.

Observable assertions: adjacent exact attribution text links to the selected canonical release; link activation does not apply/select art; five-hour simulated expiry clears transient images/data and selection; pending downloads do not save after expiry; local artwork remains; CD fallback preserves playback. Keep expired/stale data out of system Now Playing only according to the approved G-DISPLAY decision. Capture only disposable windows. New README screenshots may be captured afterward; otherwise keep the screenshot gallery absent/pending rather than retaining retired art.

##### V3 — exact final artifacts, source delivery, and commits

- Run the correct publication verifier phase before each initial/final checkpoint. Preserve the initial-copy manifest as history of preparation, not as the new release truth.
- Verify the source archive's complete exact file membership and hashes before a clean-directory rebuild. Verify all non-system dependency source inputs and receipts; a successful app build using prebuilt dylibs alone is not a vendor-source rebuild.
- Verify the staged index before the first root commit, then audit every commit in the selected publication ancestry for retired artwork/reference material. Once the clean root exists, use a green commit after each D task with `git add -- <only-that-task's-listed-paths>` and an explanatory message; do not stage unrelated constructor fixes without listing them in the task's actual path ledger.
- Update only candidate `AGENTS.md`, `README.md`, `CONTINUITY.md`, `NEXT_STEPS.md`, `TESTING.md`, `docs/LICENSE_SCOPE.md`, `docs/LICENSING_REVIEW.md`, `docs/PUBLICATION_BASELINE.md`, and the active ExecPlan as facts change. Change the outdated “no project license selected” statements only after L0/L1 are satisfied. Leave original audit evidence historical, not falsely rewritten as a past clearance.
- Archive the ExecPlan only for the acceptance scope actually completed. Keep human brand/import/system-display gates explicitly pending if unresolved. No commit/push, release upload, normal-library mutation or signing operation occurs in plan mode.

#### Final evidence and stop conditions

Stop the affected phase on an unexplained imported source/license, missing actual upstream/input proof, inability to reproduce the selected source build, new test regression, absent UI isolation, unexpected worktree drift, or unavailable approval. Do not use synthetic evidence outside explicitly labeled tests, do not claim a generic upstream URL is corresponding source, and do not erase pre-existing user covers/tags.

The source audit's reference URLs are evidence to recheck at implementation/release time, not immutable promises about third-party policy:

- GNU GPLv3 text: https://www.gnu.org/licenses/gpl-3.0.html
- FSF linking/source FAQ: https://www.gnu.org/licenses/gpl-faq.en.html
- Discogs API terms: https://support.discogs.com/hc/en-us/articles/360009334593-API-Terms-of-Use

The technical plan is complete enough to begin the named RED/GREEN slices after execution approval. S5's release-specific source commands and V2's canned-response UI fixture are explicitly gated follow-up specifications because the necessary authenticated source selection/fixture wiring has not been established; they are not invitations to improvise or claims that those gates are already satisfied. Complete those bounded specifications in the active plan before executing them.

#### Exact evidence-record commands used by the tasks

##### Record retired images before A1/A3 changes any image

Run once in the candidate after reviewing the existing preparation manifest and before replacing images. This creates only a hash denylist, not an archive of the old artwork:

```sh
python3 - <<'PY'
import json, pathlib
root = pathlib.Path.cwd()
manifest = json.loads((root / 'publication/manifest.json').read_text())
rows = [r for r in manifest['files']
        if (r['path'].startswith('Sources/Resources/') or
            r['path'].startswith('docs/screenshots/'))
        and pathlib.PurePosixPath(r['path']).suffix in {'.png', '.svg'}]
old = {r['sha256'] for r in rows}
old.add('bcf5041f86decc7e4de45c0258a72f3b727fb27a76dd11988d03383e15709105')
value = {'schema': 1, 'purpose': 'Prevent retired runtime/reference artwork in public ancestry',
         'sha256': sorted(old), 'before_paths': sorted(r['path'] for r in rows)}
path = root / 'publication/retired-asset-hashes.json'
with path.open('x', encoding='utf-8') as handle:
    json.dump(value, handle, indent=2)
    handle.write('\n')
PY
```

Expected: one new JSON record with the real old hashes. A second invocation refuses to overwrite it. Do not regenerate this file from new artwork and accidentally ban the replacements while forgetting the originals.

##### Component validation after S1–S8 evidence exists

`publication/corresponding-source.json` has top-level `{ "schema": 1, "components": [...] }`; exactly one component record per required ID. Run:

```sh
python3 - <<'PY'
import json, pathlib, sys
sys.path.insert(0, 'scripts')
from publication_checks import verify_component
root = pathlib.Path.cwd().resolve()
value = json.loads((root / 'publication/corresponding-source.json').read_text())
components = value['components']
expected = {'aubio', 'flac', 'ogg', 'grdb.swift'}
assert value['schema'] == 1
assert len(components) == len(expected)
assert {c['id'] for c in components} == expected
for component in components:
    verify_component(root, component)
print('Recorded component source inventories and evidence files verified')
PY
```

Expected before source review/rebuild: nonzero and a named unverified component. Expected after real evidence: exit zero. This verifies record consistency, not the legal sufficiency of an unauthenticated source or a manually fabricated review. The separate release receipt must bind actual app/static-link output as well as vendored dylib artifacts.

##### Copy the generated image set without touching unrelated resources

After two same-toolchain generation runs match and the retired-hash file exists:

```sh
python3 - "$RUN/art-first" <<'PY'
import hashlib, json, pathlib, shutil, sys
source = pathlib.Path(sys.argv[1]).resolve()
destination = pathlib.Path('Sources/Resources').resolve()
manifest = json.loads((source / 'asset-provenance.json').read_text())
assert manifest['method'] == 'project-authored-geometric-renderer'
assert manifest['license'] == 'GPL-3.0-or-later'
for entry in manifest['files']:
    name = entry['name']
    assert pathlib.PurePosixPath(name).name == name and name not in {'.', '..'}
    incoming = source / name
    assert incoming.is_file() and not incoming.is_symlink()
    assert hashlib.sha256(incoming.read_bytes()).hexdigest() == entry['sha256']
    shutil.copyfile(incoming, destination / name)
shutil.copyfile(source / 'asset-provenance.json', destination / 'asset-provenance.json')
PY
quick --filter 'ArtworkProvenanceTests|ThemeTests|ArtworkThumbnailServiceTests'
```

The original retired images remain only in the preserved development checkout. This operation replaces candidate image bytes only; it must not copy a hidden image archive, alter Legal/ texts, or delete user artwork.
